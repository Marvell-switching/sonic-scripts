#!/bin/bash

# This script installs all packages required for SONiC build.

# MINIMAL REQUIREMENTS to Build-Machine/Server
# --------------------------------------------
# Ubuntu-20 or Ubuntu-22 (Ubuntu-24 may not work)
# (!)NATIVE build:
#    build amd64 on Intel/x86_64/amd64 machine  
#    build arm64 on ARM64 machine  
#    build armhf on ARM64 with QEMU (refer "For ARMHF QEMU only" at the end)
# -------------------------------
#  12      CPUs
#  32GB    RAM
#  256GB   Disk-Strorage size (build is huge and long => fast SDD)

# SUDO install to be done manually
# apt update -y
# apt install -y sudo
# adduser <user>
# usermod -aG sudo <user>
# sudo su
# echo "<user> ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/<user>
# su <user>

# EDIT sudo vi /etc/security/limits.conf
#      add 2 lines
#       * soft nofile 4096
#       * hard nofile 4096



sudo apt install -y vim nano tofrodos htop sysstat lsb-release net-tools iputils-ping whois xterm ioping procps kmod binwalk jq
sudo apt install -y nfs-common

sudo apt install -y containerd
sudo apt install -y docker.io ;#may not present
sudo apt install -y docker
sudo chmod a+rw /var/run/docker.sock
sudo apt install -y build-essential
sudo apt install -y git python3-pip
sudo usermod -aG docker #==<USER>======
newgrp docker
sudo chmod a+w -R /var/cache

python3 -m pip install --user pytest

#sudo pip3 install jinja2 j2cli
sudo snap install j2 
sudo apt  install j2cli
python3 -m pip install --user jinja2 j2cli
sudo python3 -m pip install --upgrade pip #may be not present
sudo apt install -y apt-utils  python3-dev python3-yaml

sudo apt install -y debhelper stgit dh-python quilt
sudo apt-get install -y apt-transport-https ca-certificates curl gnupg wget
sudo apt install flex bison libgmp-dev libmpfr-dev libmpc-dev \
                 libisl-dev texinfo python3 zip unzip -y

sudo apt update -y
sudo apt install -y software-properties-common
sudo apt update -y
pip3 install pytest

sudo apt update -y
sudo apt-get check -y
sudo apt -y upgrade


# --- For ARMHF QEMU only ----------
# sudo apt install -y qemu ##may not present
# sudo apt install -y qemu-system qemu-utils qemu-user-static binfmt-support
# sudo update-binfmts --find /var/lib/binfmts
# sudo update-binfmts --disable qemu-arm
# sudo update-binfmts --enable  qemu-arm
# ls -al /proc/sys/fs/binfmt_misc/* #qemu-arm checking
# sudo apt install -y software-properties-common
# sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
# sudo apt install -y qemu-kvm  # Interactive, requires time-zone input
# sudo apt install -y libvirt-bin #???
# sudo apt install -y virt-manager
