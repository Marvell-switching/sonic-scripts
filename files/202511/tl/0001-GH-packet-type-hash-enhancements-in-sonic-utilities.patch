From 2f603f389fbdc81b55be94813b3f95f21c884b42 Mon Sep 17 00:00:00 2001
From: Anandhi Dhanabalan <adhanabalan@marvell.com>
Date: Thu, 11 Dec 2025 16:55:52 +0530
Subject: [PATCH] GH packet-type hash enhancements in sonic-utilities

Signed-off-by: Anandhi Dhanabalan <adhanabalan@marvell.com>
---
 config/plugins/sonic-hash.py                  | 416 +++++++++++---
 doc/Command-Reference.md                      | 135 ++---
 show/plugins/sonic-hash.py                    | 489 ++++++++++++-----
 tests/hash_input/assert_show_output.py        | 513 ++++++++++++------
 tests/hash_input/mock_state/ecmp.json         |   6 +-
 tests/hash_input/mock_state/ecmp_and_lag.json |   8 +-
 tests/hash_input/mock_state/empty.json        |   6 +-
 tests/hash_input/mock_state/lag.json          |   6 +-
 .../mock_state/no_capabilities.json           |   6 +-
 .../hash_input/mock_state/not_applicable.json |   7 +-
 .../mock_state/pkttype_disabled.json          |  15 +
 .../mock_state/pkttype_ecmp_and_lag.json      |  15 +
 .../mock_state/pkttype_limited.json           |  15 +
 .../mock_state/pkttype_limited_fields.json    |  15 +
 tests/hash_test.py                            | 456 +++++++++++++++-
 utilities_common/switch_hash.py               |  17 +-
 16 files changed, 1680 insertions(+), 445 deletions(-)
 create mode 100644 tests/hash_input/mock_state/pkttype_disabled.json
 create mode 100644 tests/hash_input/mock_state/pkttype_ecmp_and_lag.json
 create mode 100644 tests/hash_input/mock_state/pkttype_limited.json
 create mode 100644 tests/hash_input/mock_state/pkttype_limited_fields.json

diff --git a/config/plugins/sonic-hash.py b/config/plugins/sonic-hash.py
index 75a78772..409ad197 100644
--- a/config/plugins/sonic-hash.py
+++ b/config/plugins/sonic-hash.py
@@ -16,6 +16,10 @@ from utilities_common.switch_hash import (
     SW_CAP_LAG_HASH_CAPABLE_KEY,
     SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY,
     SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY,
+    SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY,
+    SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY,
+    SW_CAP_ECMP_PKT_TYPE_LIST_KEY,
+    SW_CAP_LAG_PKT_TYPE_LIST_KEY,
     SW_HASH_KEY,
     SW_CAP_KEY,
     HASH_FIELD_LIST,
@@ -27,6 +31,8 @@ from utilities_common.switch_hash import (
     to_str,
 )
 
+SUPPORTED_PKT_TYPE_LIST = ["ipv4", "ipv6", "ipnip", "ipv4-rdma", "ipv6-rdma"]
+SUPPORTED_PKT_TYPE_HELP = "Supported packet types: " + ", ".join(SUPPORTED_PKT_TYPE_LIST)
 
 log = logger.Logger(SYSLOG_IDENTIFIER)
 log.set_min_log_priority_info()
@@ -43,7 +49,7 @@ def hash_field_validator(ctx, param, value):
         param: click parameter context
         value: value of parameter
     Returns:
-        str: validated parameter
+        list: validated parameter
     """
 
     for hash_field in value:
@@ -68,7 +74,7 @@ def hash_algorithm_validator(ctx, param, value):
     return value
 
 
-def ecmp_hash_validator(ctx, db, ecmp_hash):
+def ecmp_hash_validator(ctx, db, ecmp_hash, is_pkt_type=False, pkt_type=None):
     """
     Check if ECMP hash argument is valid
 
@@ -76,39 +82,83 @@ def ecmp_hash_validator(ctx, db, ecmp_hash):
         ctx: click context
         db: State DB connector object
         ecmp_hash: ECMP hash field list
+        is_pkt_type: True when validating per-packet-type hash, False for global hash
+        pkt_type: packet-type name when is_pkt_type is True
     """
+    # Derive a safe hint even if ctx or get_param_hint misbehave
+    try:
+        hint = get_param_hint(ctx, "ecmp_hash")
+    except Exception:
+        hint = "ecmp-hash"
 
+    # 1) Duplicate check
     dup_list = get_dupes(ecmp_hash)
     if dup_list:
-        raise click.UsageError("Invalid value for {}: {} has a duplicate hash field(s) {}".format(
-            get_param_hint(ctx, "ecmp_hash"), to_str(ecmp_hash), to_str(dup_list)), ctx
+        dup_str = ", ".join(dup_list)           # "DST_MAC" or "DST_MAC, SRC_MAC"
+        fields_str = " ".join(ecmp_hash)        # full list as passed
+        raise click.UsageError(
+            f"Invalid value for {hint}: {fields_str} has a duplicate hash field(s) {dup_str}",
+            ctx,
         )
 
+    # 2) Capability checks
     entry = db.get_all(db.STATE_DB, "{}|{}".format(STATE_SWITCH_CAPABILITY, SW_CAP_KEY))
-
-    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, 'N/A')
-    entry.setdefault(SW_CAP_ECMP_HASH_CAPABLE_KEY, 'false')
-
-    if entry[SW_CAP_ECMP_HASH_CAPABLE_KEY] == 'false':
-        raise click.UsageError("Failed to configure {}: operation is not supported".format(
-            get_param_hint(ctx, "ecmp_hash")), ctx
-        )
+    if entry is None:
+        entry = {}
+    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, "N/A")
+
+    if not is_pkt_type:
+        entry.setdefault(SW_CAP_ECMP_HASH_CAPABLE_KEY, "false")
+        if entry[SW_CAP_ECMP_HASH_CAPABLE_KEY] != "true":
+            raise click.UsageError(
+                f'Failed to configure {hint}: operation is not supported',
+                ctx,
+            )
+    else:
+        entry.setdefault(SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY, "false")
+        entry.setdefault(SW_CAP_ECMP_PKT_TYPE_LIST_KEY, "")
+        if entry[SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY] != "true":
+            raise click.UsageError(
+                f'Failed to configure {hint}: packet-type operation is not supported',
+                ctx,
+            )
+        if not entry[SW_CAP_ECMP_PKT_TYPE_LIST_KEY]:
+            raise click.UsageError(
+                f'Failed to configure {hint}: no packet-type hash capabilities',
+                ctx,
+            )
+        if pkt_type:
+            # Normalize both capability list and CLI pkt-type so that
+            # names with '-' or '_' are treated equivalently, e.g.
+            # "ipv4-rdma" vs "IPV4_RDMA".
+            def _norm(name: str) -> str:
+                return name.replace("-", "_").upper()
+
+            supported = [_norm(p.strip())
+                         for p in entry[SW_CAP_ECMP_PKT_TYPE_LIST_KEY].split(",") if p.strip()]
+            if _norm(pkt_type) not in supported:
+                raise click.UsageError(
+                    f'Failed to configure {hint}: packet-type {pkt_type} is not supported',
+                    ctx,
+                )
 
     if not entry[SW_CAP_HASH_FIELD_LIST_KEY]:
-        raise click.UsageError("Failed to configure {}: no hash field capabilities".format(
-            get_param_hint(ctx, "ecmp_hash")), ctx
+        raise click.UsageError(
+            f'Failed to configure {hint}: no hash field capabilities',
+            ctx,
         )
 
-    if entry[SW_CAP_HASH_FIELD_LIST_KEY] == 'N/A':
+    if entry[SW_CAP_HASH_FIELD_LIST_KEY] == "N/A":
         return
 
-    cap_list = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(',')
-
+    cap_list = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(",")
     for hash_field in ecmp_hash:
-        click.Choice(cap_list).convert(hash_field, get_param(ctx, "ecmp_hash"), ctx)
+        click.Choice(cap_list).convert(
+            hash_field, get_param(ctx, "ecmp_hash"), ctx
+        )
 
 
-def lag_hash_validator(ctx, db, lag_hash):
+def lag_hash_validator(ctx, db, lag_hash, is_pkt_type=False, pkt_type=None):
     """
     Check if LAG hash argument is valid
 
@@ -116,37 +166,76 @@ def lag_hash_validator(ctx, db, lag_hash):
         ctx: click context
         db: State DB connector object
         lag_hash: LAG hash field list
+        is_pkt_type: True when validating per-packet-type hash, False for global hash
+        pkt_type: packet-type name when is_pkt_type is True
     """
+    try:
+        hint = get_param_hint(ctx, "lag_hash")
+    except Exception:
+        hint = "lag-hash"
 
+    # 1) Duplicate check
     dup_list = get_dupes(lag_hash)
     if dup_list:
-        raise click.UsageError("Invalid value for {}: {} has a duplicate hash field(s) {}".format(
-            get_param_hint(ctx, "lag_hash"), to_str(lag_hash), to_str(dup_list)), ctx
+        dup_str = ", ".join(dup_list)
+        fields_str = " ".join(lag_hash)
+        raise click.UsageError(
+            f"Invalid value for {hint}: {fields_str} has a duplicate hash field(s) {dup_str}",
+            ctx,
         )
 
-    entry = db.get_all(db.STATE_DB, "{}|{}".format(STATE_SWITCH_CAPABILITY, SW_CAP_KEY))
-
-    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, 'N/A')
-    entry.setdefault(SW_CAP_LAG_HASH_CAPABLE_KEY, 'false')
-
-    if entry[SW_CAP_LAG_HASH_CAPABLE_KEY] == 'false':
-        raise click.UsageError("Failed to configure {}: operation is not supported".format(
-            get_param_hint(ctx, "lag_hash")), ctx
-        )
+    # 2) Capability checks
+    entry = db.get_all(db.STATE_DB, f"{STATE_SWITCH_CAPABILITY}|{SW_CAP_KEY}")
+    if entry is None:
+        entry = {}
+    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, "N/A")
+
+    if not is_pkt_type:
+        entry.setdefault(SW_CAP_LAG_HASH_CAPABLE_KEY, "false")
+        if entry[SW_CAP_LAG_HASH_CAPABLE_KEY] != "true":
+            raise click.UsageError(
+                f'Failed to configure {hint}: operation is not supported',
+                ctx,
+            )
+    else:
+        entry.setdefault(SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY, "false")
+        entry.setdefault(SW_CAP_LAG_PKT_TYPE_LIST_KEY, "")
+        if entry[SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY] != "true":
+            raise click.UsageError(
+                f'Failed to configure {hint}: packet-type operation is not supported',
+                ctx,
+            )
+        if not entry[SW_CAP_LAG_PKT_TYPE_LIST_KEY]:
+            raise click.UsageError(
+                f'Failed to configure {hint}: no packet-type hash capabilities',
+                ctx,
+            )
+        if pkt_type:
+            def _norm(name: str) -> str:
+                return name.replace("-", "_").upper()
+
+            supported = [_norm(p.strip())
+                         for p in entry[SW_CAP_LAG_PKT_TYPE_LIST_KEY].split(",") if p.strip()]
+            if _norm(pkt_type) not in supported:
+                raise click.UsageError(
+                    f'Failed to configure {hint}: packet-type {pkt_type} is not supported',
+                    ctx,
+                )
 
     if not entry[SW_CAP_HASH_FIELD_LIST_KEY]:
-        raise click.UsageError("Failed to configure {}: no hash field capabilities".format(
-            get_param_hint(ctx, "lag_hash")), ctx
+        raise click.UsageError(
+            f'Failed to configure {hint}: no hash field capabilities',
+            ctx,
         )
 
-    if entry[SW_CAP_HASH_FIELD_LIST_KEY] == 'N/A':
+    if entry[SW_CAP_HASH_FIELD_LIST_KEY] == "N/A":
         return
 
-    cap_list = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(',')
-
+    cap_list = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(",")
     for hash_field in lag_hash:
-        click.Choice(cap_list).convert(hash_field, get_param(ctx, "lag_hash"), ctx)
-
+        click.Choice(cap_list).convert(
+            hash_field, get_param(ctx, "lag_hash"), ctx
+        )
 
 def ecmp_hash_algorithm_validator(ctx, db, ecmp_hash_algorithm):
     """
@@ -262,6 +351,29 @@ def update_entry_validated(db, table, key, data, create_if_not_exists=False):
     db.set_entry(table, key, cfg[table][key])
 
 #
+# helper functions---------------------------------------------------------------------------------------------------
+#
+def _merge_hash_fields(existing_str, new_fields):
+    """Merge hash fields preserving order, without duplicates."""
+    existing = []
+    if existing_str:
+        existing = [f.strip() for f in existing_str.split(",") if f.strip()]
+
+    result = existing[:]
+    for f in new_fields:
+        if f not in result:
+            result.append(f)
+    return result
+
+def _delete_hash_fields(existing_str, delete_fields):
+    """Delete the given fields from existing_str; return new list."""
+    if not existing_str:
+        return []
+
+    existing = [f.strip() for f in existing_str.split(",") if f.strip()]
+    delete_set = set(delete_fields)
+    return [f for f in existing if f not in delete_set]
+#
 # Hash CLI ------------------------------------------------------------------------------------------------------------
 #
 
@@ -288,62 +400,224 @@ def SWITCH_HASH_GLOBAL():
 @SWITCH_HASH_GLOBAL.command(
     name="ecmp-hash"
 )
-@click.argument(
-    "ecmp-hash",
-    nargs=-1,
-    required=True,
-    callback=hash_field_validator,
+@click.argument("hash_fields", nargs=-1, type=click.Choice(HASH_FIELD_LIST), required=False, metavar="ECMP_HASH")
+@click.option(
+    "--packet-type",
+    type=click.Choice(SUPPORTED_PKT_TYPE_LIST, case_sensitive=False),
+    help="Configures the hash for given packet type. " + SUPPORTED_PKT_TYPE_HELP
+)
+@click.option(
+    "--action",
+    type=click.Choice(["add", "del"]),
+    help=(
+        "When used with --packet-type: 'del' removes hash fields (or deletes "
+        "the packet-type hash if no fields are given); 'add' merges new hash "
+        "fields into the existing list. If omitted, the given fields "
+        "overwrite any existing list for that packet-type."
+    ),
 )
 @clicommon.pass_db
 @click.pass_context
-def SWITCH_HASH_GLOBAL_ecmp_hash(ctx, db, ecmp_hash):
+def ecmp_hash(ctx, db, hash_fields, packet_type, action):
     """ Hash fields for hashing packets going through ECMP """
 
-    ecmp_hash_validator(ctx, db.db, ecmp_hash)
+    # Validate option combinations
+    # When --packet-type is specified:
+    #   - --action del : mandatory for delete semantics
+    #   - --action add : optional; when present, merge; when omitted, overwrite
+    if action and not packet_type:
+        ctx.fail("--packet-type is required when --action is specified")
 
+    cfgdb = db.cfgdb
     table = CFG_SWITCH_HASH
     key = SW_HASH_KEY
-    data = {
-        "ecmp_hash": ecmp_hash,
-    }
-
-    try:
-        update_entry_validated(db.cfgdb, table, key, data, create_if_not_exists=True)
-        log.log_notice("Configured switch global ECMP hash: {}".format(to_str(ecmp_hash)))
-    except Exception as e:
-        log.log_error("Failed to configure switch global ECMP hash: {}".format(str(e)))
-        ctx.fail(str(e))
 
+    # Packet-type specific configuration
+    if packet_type:
+        # Upper-case form is used for capability validation and logs
+        pkt_type_norm = packet_type.upper()
+        # CONFIG_DB attribute key is always lower-case with '-' normalized to '_'
+        pkt_key = packet_type.lower().replace("-", "_")
+        attr_name = f"ecmp_hash_{pkt_key}"
+        global_entry = cfgdb.get_entry(table, key) or {}
+        existing_value = global_entry.get(attr_name, "")
+
+        if action == "del":
+            # Delete semantics (unchanged):
+            #   - with hash_fields: remove subset
+            #   - without hash_fields: delete entire pkt-type attribute
+            if not hash_fields:
+                if existing_value:
+                    update_entry_validated(cfgdb, table, key, {attr_name: None}, create_if_not_exists=True)
+                    log.log_notice(f"Removed ECMP hash configuration for {packet_type}")
+                return
+
+            new_list = _delete_hash_fields(existing_value, list(hash_fields))
+            new_value = ",".join(new_list) if new_list else ""
+
+            if not new_list:
+                update_entry_validated(cfgdb, table, key, {attr_name: None}, create_if_not_exists=True)
+                log.log_notice(f"Removed ECMP hash configuration for {packet_type}")
+            elif new_value != existing_value:
+                update_entry_validated(cfgdb, table, key, {attr_name: new_value}, create_if_not_exists=True)
+                log.log_notice(f"Updated ECMP hash for {packet_type}: removed {to_str(hash_fields)}")
+        else:
+            # ADD / overwrite semantics:
+            #   - action == "add": merge with existing (ignore duplicates)
+            #   - action is None: overwrite any existing list
+            if not hash_fields:
+                available_fields = ", ".join(HASH_FIELD_LIST)
+                ctx.fail(
+                    "Hash fields are required when --packet-type is specified "
+                    "and --action is not 'del'\n\nAvailable hash fields:\n"
+                    f"{available_fields}"
+                )
+
+            hash_fields = list(hash_fields)
+            ecmp_hash_validator(ctx, db.db, hash_fields, is_pkt_type=True, pkt_type=pkt_type_norm)
+
+            if action == "add":
+                merged_value = _merge_hash_fields(existing_value, hash_fields)
+            else:
+                merged_value = hash_fields
+
+            new_value = ",".join(merged_value)
+
+            if new_value != existing_value:
+                update_entry_validated(cfgdb, table, key, {attr_name: new_value}, create_if_not_exists=True)
+                if action == "add":
+                    log.log_notice(f"Configured ECMP hash for {packet_type}: {to_str(hash_fields)}")
+                else:
+                    log.log_notice(f"Set ECMP hash for {packet_type}: {to_str(hash_fields)}")
+    else:
+        # Global hash configuration
+        if not hash_fields:
+            # Show help if no arguments provided
+            click.echo(ctx.get_help())
+            ctx.exit(0)
+
+        hash_fields = list(hash_fields)
+        ecmp_hash_validator(ctx, db.db, hash_fields, is_pkt_type=False)
+
+        data = {"ecmp_hash": hash_fields}
+
+        try:
+            update_entry_validated(cfgdb, table, key, data, create_if_not_exists=True)
+            log.log_notice("Configured switch global ECMP hash: {}".format(to_str(hash_fields)))
+        except Exception as e:
+            log.log_error("Failed to configure switch global ECMP hash: {}".format(str(e)))
+            ctx.fail(str(e))
 
 @SWITCH_HASH_GLOBAL.command(
     name="lag-hash"
 )
-@click.argument(
-    "lag-hash",
-    nargs=-1,
-    required=True,
-    callback=hash_field_validator,
+@click.argument("hash_fields", nargs=-1, type=click.Choice(HASH_FIELD_LIST), required=False, metavar="LAG_HASH")
+@click.option(
+    "--packet-type",
+    type=click.Choice(SUPPORTED_PKT_TYPE_LIST, case_sensitive=False),
+    help="Configures the hash for given packet type. " + SUPPORTED_PKT_TYPE_HELP
+)
+@click.option(
+    "--action",
+    type=click.Choice(["add", "del"]),
+    help=(
+        "When used with --packet-type: 'del' removes hash fields (or deletes "
+        "the packet-type hash if no fields are given); 'add' merges new hash "
+        "fields into the existing list. If omitted, the given fields "
+        "overwrite any existing list for that packet-type."
+    ),
 )
 @clicommon.pass_db
 @click.pass_context
-def SWITCH_HASH_GLOBAL_lag_hash(ctx, db, lag_hash):
+def lag_hash(ctx, db, hash_fields, packet_type, action):
     """ Hash fields for hashing packets going through LAG """
 
-    lag_hash_validator(ctx, db.db, lag_hash)
+    # Validate option combinations
+    # When --packet-type is specified:
+    #   - --action del : mandatory for delete semantics
+    #   - --action add : optional; when present, merge; when omitted, overwrite
+    if action and not packet_type:
+        ctx.fail("--packet-type is required when --action is specified")
 
+    cfgdb = db.cfgdb
     table = CFG_SWITCH_HASH
     key = SW_HASH_KEY
-    data = {
-        "lag_hash": lag_hash,
-    }
-
-    try:
-        update_entry_validated(db.cfgdb, table, key, data, create_if_not_exists=True)
-        log.log_notice("Configured switch global LAG hash: {}".format(to_str(lag_hash)))
-    except Exception as err:
-        log.log_error("Failed to configure switch global LAG hash: {}".format(str(err)))
-        ctx.fail(str(err))
 
+    # Packet-type specific configuration
+    if packet_type:
+        # Upper-case form is used for capability validation and logs
+        pkt_type_norm = packet_type.upper()
+        # CONFIG_DB attribute key is always lower-case with '-' normalized to '_'
+        pkt_key = packet_type.lower().replace("-", "_")
+        attr_name = f"lag_hash_{pkt_key}"
+        global_entry = cfgdb.get_entry(table, key) or {}
+        existing_value = global_entry.get(attr_name, "")
+
+        if action == "del":
+            # Delete semantics (unchanged):
+            #   - with hash_fields: remove subset
+            #   - without hash_fields: delete entire pkt-type attribute
+            if not hash_fields:
+                if existing_value:
+                    update_entry_validated(cfgdb, table, key, {attr_name: None}, create_if_not_exists=True)
+                    log.log_notice(f"Removed LAG hash configuration for {packet_type}")
+                return
+
+            new_list = _delete_hash_fields(existing_value, list(hash_fields))
+            new_value = ",".join(new_list) if new_list else ""
+
+            if not new_list:
+                update_entry_validated(cfgdb, table, key, {attr_name: None}, create_if_not_exists=True)
+                log.log_notice(f"Removed LAG hash configuration for {packet_type}")
+            elif new_value != existing_value:
+                update_entry_validated(cfgdb, table, key, {attr_name: new_value}, create_if_not_exists=True)
+                log.log_notice(f"Updated LAG hash for {packet_type}: removed {to_str(hash_fields)}")
+        else:
+            # ADD / overwrite semantics:
+            #   - action == "add": merge with existing (ignore duplicates)
+            #   - action is None: overwrite any existing list
+            if not hash_fields:
+                available_fields = ", ".join(HASH_FIELD_LIST)
+                ctx.fail(
+                    "Hash fields are required when --packet-type is specified "
+                    "and --action is not 'del'\n\nAvailable hash fields:\n"
+                    f"{available_fields}"
+                )
+
+            hash_fields = list(hash_fields)
+            lag_hash_validator(ctx, db.db, hash_fields, is_pkt_type=True, pkt_type=pkt_type_norm)
+
+            if action == "add":
+                merged_value = _merge_hash_fields(existing_value, hash_fields)
+            else:
+                merged_value = hash_fields
+
+            new_value = ",".join(merged_value)
+
+            if new_value != existing_value:
+                update_entry_validated(cfgdb, table, key, {attr_name: new_value}, create_if_not_exists=True)
+                if action == "add":
+                    log.log_notice(f"Configured LAG hash for {packet_type}: {to_str(hash_fields)}")
+                else:
+                    log.log_notice(f"Set LAG hash for {packet_type}: {to_str(hash_fields)}")
+    else:
+        # Global hash configuration
+        if not hash_fields:
+            # Show help if no arguments provided
+            click.echo(ctx.get_help())
+            ctx.exit(0)
+
+        hash_fields = list(hash_fields)
+        lag_hash_validator(ctx, db.db, hash_fields, is_pkt_type=False)
+
+        data = {"lag_hash": hash_fields}
+
+        try:
+            update_entry_validated(cfgdb, table, key, data, create_if_not_exists=True)
+            log.log_notice("Configured switch global LAG hash: {}".format(to_str(hash_fields)))
+        except Exception as e:
+            log.log_error("Failed to configure switch global LAG hash: {}".format(str(e)))
+            ctx.fail(str(e))
 
 @SWITCH_HASH_GLOBAL.command(
     name="ecmp-hash-algorithm"
diff --git a/doc/Command-Reference.md b/doc/Command-Reference.md
index acb4963b..72240a0f 100644
--- a/doc/Command-Reference.md
+++ b/doc/Command-Reference.md
@@ -5194,57 +5194,61 @@ This command displays switch hash capabilities.
 - Example:
   ```bash
   admin@sonic:~$ show switch-hash capabilities
-  +--------+-------------------------------------+
-  | Hash   | Capabilities                        |
-  +========+=====================================+
-  | ECMP   | +-------------------+-------------+ |
-  |        | | Hash Field        | Algorithm   | |
-  |        | |-------------------+-------------| |
-  |        | | IN_PORT           | CRC         | |
-  |        | | DST_MAC           | XOR         | |
-  |        | | SRC_MAC           | RANDOM      | |
-  |        | | ETHERTYPE         | CRC_32LO    | |
-  |        | | VLAN_ID           | CRC_32HI    | |
-  |        | | IP_PROTOCOL       | CRC_CCITT   | |
-  |        | | DST_IP            | CRC_XOR     | |
-  |        | | SRC_IP            |             | |
-  |        | | L4_DST_PORT       |             | |
-  |        | | L4_SRC_PORT       |             | |
-  |        | | INNER_DST_MAC     |             | |
-  |        | | INNER_SRC_MAC     |             | |
-  |        | | INNER_ETHERTYPE   |             | |
-  |        | | INNER_IP_PROTOCOL |             | |
-  |        | | INNER_DST_IP      |             | |
-  |        | | INNER_SRC_IP      |             | |
-  |        | | INNER_L4_DST_PORT |             | |
-  |        | | INNER_L4_SRC_PORT |             | |
-  |        | | IPV6_FLOW_LABEL   |             | |
-  |        | +-------------------+-------------+ |
-  +--------+-------------------------------------+
-  | LAG    | +-------------------+-------------+ |
-  |        | | Hash Field        | Algorithm   | |
-  |        | |-------------------+-------------| |
-  |        | | IN_PORT           | CRC         | |
-  |        | | DST_MAC           | XOR         | |
-  |        | | SRC_MAC           | RANDOM      | |
-  |        | | ETHERTYPE         | CRC_32LO    | |
-  |        | | VLAN_ID           | CRC_32HI    | |
-  |        | | IP_PROTOCOL       | CRC_CCITT   | |
-  |        | | DST_IP            | CRC_XOR     | |
-  |        | | SRC_IP            |             | |
-  |        | | L4_DST_PORT       |             | |
-  |        | | L4_SRC_PORT       |             | |
-  |        | | INNER_DST_MAC     |             | |
-  |        | | INNER_SRC_MAC     |             | |
-  |        | | INNER_ETHERTYPE   |             | |
-  |        | | INNER_IP_PROTOCOL |             | |
-  |        | | INNER_DST_IP      |             | |
-  |        | | INNER_SRC_IP      |             | |
-  |        | | INNER_L4_DST_PORT |             | |
-  |        | | INNER_L4_SRC_PORT |             | |
-  |        | | IPV6_FLOW_LABEL   |             | |
-  |        | +-------------------+-------------+ |
-  +--------+-------------------------------------+
+  +--------+---------------------------------------------------+
+  | Hash   | Capabilities                                      |
+  +========+===================================================+
+  | ECMP   | +-------------------+-------------+-------------+ |
+  |        | | Hash Field        | Algorithm   | Packet Type | |
+  |        | |-------------------+-------------+-------------| |
+  |        | | IN_PORT           | CRC         | ipv4        | |
+  |        | | DST_MAC           | XOR         | ipv4        | |
+  |        | | SRC_MAC           | RANDOM      | ipnip       | |
+  |        | | ETHERTYPE         | CRC_32LO    | ipv4_rdma   | |
+  |        | | VLAN_ID           | CRC_32HI    | ipv6_rdma   | |
+  |        | | IP_PROTOCOL       | CRC_CCITT   |             | |
+  |        | | DST_IP            | CRC_XOR     |             | |
+  |        | | SRC_IP            |             |             | |
+  |        | | L4_DST_PORT       |             |             | |
+  |        | | L4_SRC_PORT       |             |             | |
+  |        | | INNER_DST_MAC     |             |             | |
+  |        | | INNER_SRC_MAC     |             |             | |
+  |        | | INNER_ETHERTYPE   |             |             | |
+  |        | | INNER_IP_PROTOCOL |             |             | |
+  |        | | INNER_DST_IP      |             |             | |
+  |        | | INNER_SRC_IP      |             |             | |
+  |        | | INNER_L4_DST_PORT |             |             | |
+  |        | | INNER_L4_SRC_PORT |             |             | |
+  |        | | IPV6_FLOW_LABEL   |             |             | |
+  |        | | RDMA_BTH_OPCODE   |             |             | |
+  |        | | RDMA_BTH_DEST_QP  |             |             | |
+  |        | +-------------------+-------------+-------------+ |
+  +--------+---------------------------------------------------+
+  | LAG    | +-------------------+-------------+-------------+ |
+  |        | | Hash Field        | Algorithm   | Packet Type | |
+  |        | |-------------------+-------------+-------------| |
+  |        | | IN_PORT           | CRC         | ipv4        | |
+  |        | | DST_MAC           | XOR         | ipv4        | |
+  |        | | SRC_MAC           | RANDOM      | ipnip       | |
+  |        | | ETHERTYPE         | CRC_32LO    | ipv4_rdma   | |
+  |        | | VLAN_ID           | CRC_32HI    | ipv6_rdma   | |
+  |        | | IP_PROTOCOL       | CRC_CCITT   |             | |
+  |        | | DST_IP            | CRC_XOR     |             | |
+  |        | | SRC_IP            |             |             | |
+  |        | | L4_DST_PORT       |             |             | |
+  |        | | L4_SRC_PORT       |             |             | |
+  |        | | INNER_DST_MAC     |             |             | |
+  |        | | INNER_SRC_MAC     |             |             | |
+  |        | | INNER_ETHERTYPE   |             |             | |
+  |        | | INNER_IP_PROTOCOL |             |             | |
+  |        | | INNER_DST_IP      |             |             | |
+  |        | | INNER_SRC_IP      |             |             | |
+  |        | | INNER_L4_DST_PORT |             |             | |
+  |        | | INNER_L4_SRC_PORT |             |             | |
+  |        | | IPV6_FLOW_LABEL   |             |             | |
+  |        | | RDMA_BTH_OPCODE   |             |             | |
+  |        | | RDMA_BTH_DEST_QP  |             |             | |
+  |        | +-------------------+-------------+-------------+ |
+  +--------+---------------------------------------------------+
   ```
 
 ### Hash Config Commands
@@ -5253,18 +5257,23 @@ This subsection explains how to configure switch hash.
 
 **config switch-hash global ecmp/lag hash**
 
-This command is used to manage switch hash global configuration.
+This command is used to manage switch hash global and switch hash packet-type configuration.
 
 - Usage:
   ```bash
-  config switch-hash global ecmp-hash <hash_field_list>
-  config switch-hash global lag-hash <hash_field_list>
-  ```
-
-- Parameters:
-  - _hash_field_list_: hash fields for hashing packets going through ECMP/LAG
+  config switch-hash global ecmp-hash [OPTIONS] ECMP_HASH
+  config switch-hash global lag-hash [OPTIONS] LAG_HASH
 
-- Examples:
+Options:
+  --packet-type [ipv4|ipv6|ipnip|ipv4-rdma|ipv6-rdma]
+                                  Configures the hash for given packet type.
+                                  Supported packet types: ipv4, ipv6, ipnip,
+                                  ipv4-rdma, ipv6-rdma
+  --action [add|del]              Action to perform: add hash fields or delete
+                                  hash fields
+  -?, -h, --help                  Show this message and exit.
+  ```
+ Examples:
   ```bash
   admin@sonic:~$ config switch-hash global ecmp-hash \
   'DST_MAC' \
@@ -5283,7 +5292,9 @@ This command is used to manage switch hash global configuration.
   'INNER_SRC_IP' \
   'INNER_L4_DST_PORT' \
   'INNER_L4_SRC_PORT' \
-  'IPV6_FLOW_LABEL'
+  'IPV6_FLOW_LABEL' \
+  'RDMA_BTH_OPCODE' \
+  'RDMA_BTH_DEST_QP'
   admin@sonic:~$ config switch-hash global lag-hash \
   'DST_MAC' \
   'SRC_MAC' \
@@ -5301,7 +5312,9 @@ This command is used to manage switch hash global configuration.
   'INNER_SRC_IP' \
   'INNER_L4_DST_PORT' \
   'INNER_L4_SRC_PORT' \
-  'IPV6_FLOW_LABEL'
+  'IPV6_FLOW_LABEL' \
+  'RDMA_BTH_OPCODE' \
+  'RDMA_BTH_DEST_QP'
   ```
 
 **config switch-hash global ecmp/lag hash algorithm**
diff --git a/show/plugins/sonic-hash.py b/show/plugins/sonic-hash.py
index 37440677..f6de4582 100644
--- a/show/plugins/sonic-hash.py
+++ b/show/plugins/sonic-hash.py
@@ -17,10 +17,20 @@ from utilities_common.switch_hash import (
     SW_CAP_LAG_HASH_CAPABLE_KEY,
     SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY,
     SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY,
+    SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY,
+    SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY,
+    SW_CAP_ECMP_PKT_TYPE_LIST_KEY,
+    SW_CAP_LAG_PKT_TYPE_LIST_KEY,
     SW_HASH_KEY,
     SW_CAP_KEY,
 )
 
+SUPPORTED_PKT_TYPE_HELP = (
+    "Supported packet-type: all, ipv4, ipv6, ipnip, ipv4-rdma, ipv6-rdma "
+)
+
+USAGE_GLOBAL = "Usage: show switch-hash global [OPTIONS] [packet-type <pkt-type>]"
+
 #
 # Hash helpers --------------------------------------------------------------------------------------------------------
 #
@@ -64,23 +74,26 @@ def SWITCH_HASH():
     is_flag=True,
     default=False
 )
+@click.argument("args", nargs=-1)
 @clicommon.pass_db
 @click.pass_context
-def SWITCH_HASH_GLOBAL(ctx, db, json_format):
-    """ Show switch hash global configuration """
-
-    header = [
-        "Hash",
-        "Configuration",
-    ]
-    body = []
-
-    sub_header = [
-        "Hash Field",
-        "Algorithm"
-    ]
-    ecmp_body = []
-    lag_body = []
+def SWITCH_HASH_GLOBAL(ctx, db, json_format, args):
+    """ Show switch hash global configuration. """
+
+    # Parse args manually to avoid Click's auto-generated usage errors
+    packet_type_keyword = args[0] if len(args) > 0 else None
+    pkt_type = args[1] if len(args) > 1 else None
+
+    # Reject extra arguments with proper usage message
+    if len(args) > 2:
+        click.echo(USAGE_GLOBAL)
+        click.echo(f"Error: Got unexpected extra argument: {args[2]}")
+        ctx.exit(1)
+
+    # Handle '?' for help (Click handles -h/--help automatically)
+    if packet_type_keyword == "?":
+        click.echo(ctx.get_help())
+        ctx.exit(0)
 
     table = db.cfgdb.get_table(CFG_SWITCH_HASH)
     entry = table.get(SW_HASH_KEY, {})
@@ -89,6 +102,28 @@ def SWITCH_HASH_GLOBAL(ctx, db, json_format):
         click.echo("No configuration is present in CONFIG DB")
         ctx.exit(0)
 
+    # Normalize pkt-type arg
+    show_pkt_type = None
+    show_all_pkt_types = False
+
+    if packet_type_keyword is not None:
+        if packet_type_keyword.lower() != "packet-type":
+            click.echo(USAGE_GLOBAL)
+            click.echo('Error: Expected "packet-type" keyword')
+            ctx.exit(1)
+
+        # User typed: show switch-hash global packet-type <ENTER> or packet-type ?
+        if not pkt_type or pkt_type == "?":
+            click.echo(USAGE_GLOBAL)
+            click.echo("")
+            click.echo(SUPPORTED_PKT_TYPE_HELP)
+            ctx.exit(0)
+
+        if pkt_type.lower() == "all":
+            show_all_pkt_types = True
+        else:
+            show_pkt_type = pkt_type.upper()
+
     if json_format:
         json_dict = {
             "ecmp": {
@@ -103,46 +138,209 @@ def SWITCH_HASH_GLOBAL(ctx, db, json_format):
         click.echo(json.dumps(json_dict, indent=4))
         ctx.exit(0)
 
-    ecmp_row = [
-        format_attr_value(
-            entry,
-            {
-                'name': 'ecmp_hash',
-                'is-leaf-list': True
-            }
-        ),
-        format_attr_value(
-            entry,
-            {
-                'name': 'ecmp_hash_algorithm',
-                'is-leaf-list': False
-            }
-        ),
-    ]
-    ecmp_body.append(ecmp_row)
-
-    lag_row = [
-        format_attr_value(
-            entry,
-            {
-                'name': 'lag_hash',
-                'is-leaf-list': True
-            }
-        ),
-        format_attr_value(
-            entry,
-            {
-                'name': 'lag_hash_algorithm',
-                'is-leaf-list': False
-            }
-        ),
-    ]
-    lag_body.append(lag_row)
-
-    body.append(["ECMP", tabulate.tabulate(ecmp_body, sub_header, "psql")])
-    body.append(["LAG", tabulate.tabulate(lag_body, sub_header, "psql")])
+    # No pkt-type: legacy global view
+    if not show_pkt_type and not show_all_pkt_types:
+        header = ["Hash", "Configuration"]
+        sub_header = ["Hash Field", "Algorithm"]
+        ecmp_body = []
+        lag_body = []
+
+        ecmp_row = [
+            format_attr_value(
+                entry,
+                {
+                    'name': 'ecmp_hash',
+                    'is-leaf-list': True
+                }
+            ),
+            format_attr_value(
+                entry,
+                {
+                    'name': 'ecmp_hash_algorithm',
+                    'is-leaf-list': False
+                }
+            ),
+        ]
+        ecmp_body.append(ecmp_row)
+
+        lag_row = [
+            format_attr_value(
+                entry,
+                {
+                    'name': 'lag_hash',
+                    'is-leaf-list': True
+                }
+            ),
+            format_attr_value(
+                entry,
+                {
+                    'name': 'lag_hash_algorithm',
+                    'is-leaf-list': False
+                }
+            ),
+        ]
+        lag_body.append(lag_row)
+
+
+        body = []
+        body.append(["ECMP", tabulate.tabulate(ecmp_body, sub_header, "psql")])
+        body.append(["LAG", tabulate.tabulate(lag_body, sub_header, "psql")])
+
+        click.echo(tabulate.tabulate(body, header, "grid"))
+        return
+
+    # pkt-type view: single pkt-type or all
+    header = ["Hash", "Packet", "Configuration"]
+    # In packet-type view, show only hash fields; algorithms are visible
+    # in the legacy global view and are not repeated per packet-type.
+    sub_header = ["Hash Field"]
+
+    def extract_pkt_type_from_attr(attr_name, prefix):
+        suffix = attr_name[len(prefix):]
+        return suffix.upper()
+
+    ecmp_pkt_map = {}
+    lag_pkt_map = {}
+    for k, v in entry.items():
+        # Per-packet-type hashes are stored as ecmp_hash_<PKT>, lag_hash_<PKT>.
+        # Exclude the global algorithm attributes (ecmp_hash_algorithm, lag_hash_algorithm)
+        # which also start with these prefixes and would otherwise appear as a
+        # synthetic "algorithm" packet-type row.
+        if k.startswith("ecmp_hash_"):
+            suffix = k[len("ecmp_hash_"):]
+            if suffix.upper() == "ALGORITHM":
+                continue
+            pt = suffix.upper()
+            ecmp_pkt_map[pt] = v
+        elif k.startswith("lag_hash_"):
+            suffix = k[len("lag_hash_"):]
+            if suffix.upper() == "ALGORITHM":
+                continue
+            pt = suffix.upper()
+            lag_pkt_map[pt] = v
+
+    # Decide which packet-types to show
+    if show_all_pkt_types:
+        pkt_types = sorted(set(list(ecmp_pkt_map.keys()) + list(lag_pkt_map.keys())))
+    else:
+        pkt_types = [show_pkt_type]
+
+    # If there is no per-packet-type configuration, exit gracefully
+    if not pkt_types:
+        click.echo("No per-packet-type hash configuration present")
+        ctx.exit(0)
 
-    click.echo(tabulate.tabulate(body, header, "grid"))
+    # Helper that formats a single hash/pkt-type config mini-table
+    def make_cfg_cell(value: str):
+        fields = [f.strip() for f in value.split(",") if f.strip()] if value else []
+        row = ["\n".join(fields) if fields else "N/A"]
+        return tabulate.tabulate([row], sub_header, "psql")
+
+    # Precompute all rows for ECMP and LAG so we can size columns
+    def collect_rows_for_hash(hash_name, pkt_map):
+        rows = []  # list of (hash_label, pkt_type_str, cfg_lines)
+        first = True
+        for pt in pkt_types:
+            val = pkt_map.get(pt) or ""
+            cell = make_cfg_cell(val)
+            cfg_lines = cell.split("\n")
+            rows.append((hash_name if first else "", pt.lower(), cfg_lines))
+            first = False
+        return rows
+
+    ecmp_rows = collect_rows_for_hash("ECMP", ecmp_pkt_map)
+    lag_rows = collect_rows_for_hash("LAG", lag_pkt_map)
+
+    # Compute column widths based on all data (content width only; we'll add padding)
+    def col_width(initial, values):
+        return max(initial, max((len(v) for v in values), default=0))
+
+    hash_col_width = col_width(len(header[0]), [r[0] for r in ecmp_rows + lag_rows])
+    # Align Hash column width with the global view (a bit wider)
+    hash_col_width = max(hash_col_width, len(header[0]) + 2)
+    pkt_col_width = col_width(len(header[1]), [r[1] for r in ecmp_rows + lag_rows])
+
+    cfg_width_candidates = [len(header[2])]
+    for _, _, cfg_lines in ecmp_rows + lag_rows:
+        for line in cfg_lines:
+            cfg_width_candidates.append(len(line))
+    cfg_col_width = max(cfg_width_candidates) if cfg_width_candidates else len(header[2])
+
+    def full_border(ch="-"):
+        # Outer border uses content width + 2 spaces for padding
+        return (
+            "+" + ch * (hash_col_width + 2) +
+            "+" + ch * (pkt_col_width + 2) +
+            "+" + ch * (cfg_col_width + 2) + "+"
+        )
+
+    def header_sep():
+        # Header separator mirrors full border but with '='
+        return (
+            "+" + "=" * (hash_col_width + 2) +
+            "+" + "=" * (pkt_col_width + 2) +
+            "+" + "=" * (cfg_col_width + 2) + "+"
+        )
+
+    def partial_sep():
+        # Separator between pkt-types for the same hash: only under Pkt-Type + Config
+        return (
+            "|" + " " * (hash_col_width + 2) +
+            "|" + "-" * (pkt_col_width + 2) +
+            "+" + "-" * (cfg_col_width + 2) + "+"
+        )
+
+    def print_group(rows):
+        # rows: list of (hash_label, pkt_type_str, cfg_lines)
+        for idx, (hash_label, pt_str, cfg_lines) in enumerate(rows):
+            # First line with headers for this pkt-type
+            cfg_first = cfg_lines[0] if cfg_lines else ""
+            click.echo(
+                "| "
+                + hash_label.ljust(hash_col_width)
+                + " | "
+                + pt_str.ljust(pkt_col_width)
+                + " | "
+                + cfg_first.ljust(cfg_col_width)
+                + " |"
+            )
+
+            # Remaining lines of the config mini-table (if any)
+            for extra_line in cfg_lines[1:]:
+                click.echo(
+                    "| "
+                    + "".ljust(hash_col_width)
+                    + " | "
+                    + "".ljust(pkt_col_width)
+                    + " | "
+                    + extra_line.ljust(cfg_col_width)
+                    + " |"
+                )
+
+            # Between pkt-types in the same hash, draw a partial separator
+            if idx != len(rows) - 1:
+                click.echo(partial_sep())
+
+    # Print the full table for ECMP and LAG blocks
+    click.echo(full_border("-"))
+    click.echo(
+        "| "
+        + header[0].ljust(hash_col_width)
+        + " | "
+        + header[1].ljust(pkt_col_width)
+        + " | "
+        + header[2].ljust(cfg_col_width)
+        + " |"
+    )
+    click.echo(header_sep())
+
+    # ECMP block
+    print_group(ecmp_rows)
+    click.echo(full_border("-"))
+
+    # LAG block
+    print_group(lag_rows)
+    click.echo(full_border("-"))
 
 
 @SWITCH_HASH.command(
@@ -167,7 +365,8 @@ def SWITCH_HASH_CAPABILITIES(ctx, db, json_format):
 
     sub_header = [
         "Hash Field",
-        "Algorithm"
+        "Algorithm",
+        "Packet Type"
     ]
     ecmp_body = []
     lag_body = []
@@ -175,86 +374,132 @@ def SWITCH_HASH_CAPABILITIES(ctx, db, json_format):
     entry = db.db.get_all(db.db.STATE_DB, "{}|{}".format(STATE_SWITCH_CAPABILITY, SW_CAP_KEY))
 
     if not entry:
-        ctx.fail("No data is present in STATE DB")
-
-    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, 'N/A')
-    entry.setdefault(SW_CAP_ECMP_HASH_ALGORITHM_KEY, 'N/A')
-    entry.setdefault(SW_CAP_LAG_HASH_ALGORITHM_KEY, 'N/A')
-    entry.setdefault(SW_CAP_ECMP_HASH_CAPABLE_KEY, 'false')
-    entry.setdefault(SW_CAP_LAG_HASH_CAPABLE_KEY, 'false')
-    entry.setdefault(SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY, 'false')
-    entry.setdefault(SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY, 'false')
-
-    if not entry[SW_CAP_HASH_FIELD_LIST_KEY]:
-        entry[SW_CAP_HASH_FIELD_LIST_KEY] = "no capabilities"
-
-    if not entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY]:
-        entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY] = "no capabilities"
-
-    if not entry[SW_CAP_LAG_HASH_ALGORITHM_KEY]:
-        entry[SW_CAP_LAG_HASH_ALGORITHM_KEY] = "no capabilities"
+        click.echo("Error: No data is present in STATE DB")
+        ctx.exit(1)
+
+
+    # Normalize raw values
+    entry.setdefault(SW_CAP_HASH_FIELD_LIST_KEY, "")
+    entry.setdefault(SW_CAP_ECMP_HASH_ALGORITHM_KEY, "")
+    entry.setdefault(SW_CAP_LAG_HASH_ALGORITHM_KEY, "")
+    entry.setdefault(SW_CAP_ECMP_PKT_TYPE_LIST_KEY, "")
+    entry.setdefault(SW_CAP_LAG_PKT_TYPE_LIST_KEY, "")
+
+    # Convert "" / "N/A" to normalized strings
+    def normalize(raw: str) -> str:
+        if raw == "N/A":
+            return "N/A"
+        if raw == "":
+            return "no capabilities"
+        return raw
+
+    hash_field_str = normalize(entry[SW_CAP_HASH_FIELD_LIST_KEY])
+    ecmp_algo_str = normalize(entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY])
+    lag_algo_str = normalize(entry[SW_CAP_LAG_HASH_ALGORITHM_KEY])
+    ecmp_pkt_str = normalize(entry[SW_CAP_ECMP_PKT_TYPE_LIST_KEY])
+    lag_pkt_str = normalize(entry[SW_CAP_LAG_PKT_TYPE_LIST_KEY])
 
     if json_format:
-        if entry[SW_CAP_HASH_FIELD_LIST_KEY] not in ["N/A", "no capabilities"]:
-            entry[SW_CAP_HASH_FIELD_LIST_KEY] = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(',')
-
-        if entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY] not in ["N/A", "no capabilities"]:
-            entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY] = entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY].split(',')
-
-        if entry[SW_CAP_LAG_HASH_ALGORITHM_KEY] not in ["N/A", "no capabilities"]:
-            entry[SW_CAP_LAG_HASH_ALGORITHM_KEY] = entry[SW_CAP_LAG_HASH_ALGORITHM_KEY].split(',')
+        def list_or_str(raw: str):
+            # Use same normalization as table view; then convert to list where appropriate
+            raw = normalize(raw)
+            if raw in ("N/A", "no capabilities"):
+                return raw
+            return [x.strip() for x in raw.split(",")] if raw else "no capabilities"
 
         json_dict = {
             "ecmp": {
-                "hash_field": entry[SW_CAP_HASH_FIELD_LIST_KEY] if entry[SW_CAP_ECMP_HASH_CAPABLE_KEY] == 'true' else 'not supported',
-                "algorithm": entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY] if entry[SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY] == 'true' else 'not supported'
+                "hash_field": (
+                    "not supported"
+                    if entry.get(SW_CAP_ECMP_HASH_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_HASH_FIELD_LIST_KEY, ""))
+                ),
+                "algorithm": (
+                    "not supported"
+                    if entry.get(SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_ECMP_HASH_ALGORITHM_KEY, ""))
+                ),
+                "pkt_type": (
+                    "not supported"
+                    if entry.get(SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_ECMP_PKT_TYPE_LIST_KEY, ""))
+                ),
             },
             "lag": {
-                "hash_field": entry[SW_CAP_HASH_FIELD_LIST_KEY] if entry[SW_CAP_LAG_HASH_CAPABLE_KEY] == 'true' else 'not supported',
-                "algorithm": entry[SW_CAP_LAG_HASH_ALGORITHM_KEY] if entry[SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY] == 'true' else 'not supported'
-            }
+                "hash_field": (
+                    "not supported"
+                    if entry.get(SW_CAP_LAG_HASH_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_HASH_FIELD_LIST_KEY, ""))
+                ),
+                "algorithm": (
+                    "not supported"
+                    if entry.get(SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_LAG_HASH_ALGORITHM_KEY, ""))
+                ),
+                "pkt_type": (
+                    "not supported"
+                    if entry.get(SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY, "true") == "false"
+                    else list_or_str(entry.get(SW_CAP_LAG_PKT_TYPE_LIST_KEY, ""))
+                ),
+            },
         }
         click.echo(json.dumps(json_dict, indent=4))
         ctx.exit(0)
 
-    entry[SW_CAP_HASH_FIELD_LIST_KEY] = entry[SW_CAP_HASH_FIELD_LIST_KEY].split(',')
-    entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY] = entry[SW_CAP_ECMP_HASH_ALGORITHM_KEY].split(',')
-    entry[SW_CAP_LAG_HASH_ALGORITHM_KEY] = entry[SW_CAP_LAG_HASH_ALGORITHM_KEY].split(',')
-
-    ecmp_row = [
-        format_attr_value(
-            entry,
-            {
-                'name': SW_CAP_HASH_FIELD_LIST_KEY,
-                'is-leaf-list': True
-            }
-        ) if entry[SW_CAP_ECMP_HASH_CAPABLE_KEY] == 'true' else 'not supported',
-        format_attr_value(
-            entry,
-            {
-                'name': SW_CAP_ECMP_HASH_ALGORITHM_KEY,
-                'is-leaf-list': True
-            }
-        ) if entry[SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY] == 'true' else 'not supported',
-    ]
+    # Non-JSON: split only real lists, preserve N/A / no capabilities
+    def split_if_list(raw: str):
+        if raw in ("N/A", "no capabilities"):
+            return []
+        return [x.strip() for x in raw.split(",")] if raw else []
+
+    hash_field_list = split_if_list(hash_field_str)
+    ecmp_algo_list = split_if_list(ecmp_algo_str)
+    lag_algo_list = split_if_list(lag_algo_str)
+    ecmp_pkt_list = split_if_list(ecmp_pkt_str)
+    lag_pkt_list = split_if_list(lag_pkt_str)
+
+    def format_list_or_str(raw: str, lst: list) -> str:
+        # same semantics as JSON helpers: N/A & "no capabilities" stay scalar
+        if raw in ("N/A", "no capabilities"):
+            return raw
+        return "\n".join(lst) if lst else "no capabilities"
+
+    # ECMP: use capability flags per column, like JSON
+    ecmp_hash_val = (
+        "not supported"
+        if entry.get(SW_CAP_ECMP_HASH_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(hash_field_str, hash_field_list)
+    )
+    ecmp_algo_val = (
+        "not supported"
+        if entry.get(SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(ecmp_algo_str, ecmp_algo_list)
+    )
+    ecmp_pkt_val = (
+        "not supported"
+        if entry.get(SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(ecmp_pkt_str, ecmp_pkt_list)
+    )
+    ecmp_row = [ecmp_hash_val, ecmp_algo_val, ecmp_pkt_val]
     ecmp_body.append(ecmp_row)
 
-    lag_row = [
-        format_attr_value(
-            entry,
-            {
-                'name': SW_CAP_HASH_FIELD_LIST_KEY,
-                'is-leaf-list': True
-            }
-        ) if entry[SW_CAP_LAG_HASH_CAPABLE_KEY] == 'true' else 'not supported',
-        format_attr_value(
-            entry,
-            {
-                'name': SW_CAP_LAG_HASH_ALGORITHM_KEY,
-                'is-leaf-list': True
-            }
-        ) if entry[SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY] == 'true' else 'not supported',
-    ]
+    # LAG: same pattern
+    lag_hash_val = (
+        "not supported"
+        if entry.get(SW_CAP_LAG_HASH_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(hash_field_str, hash_field_list)
+    )
+    lag_algo_val = (
+        "not supported"
+        if entry.get(SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(lag_algo_str, lag_algo_list)
+    )
+    lag_pkt_val = (
+        "not supported"
+        if entry.get(SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY, "true") == "false"
+        else format_list_or_str(lag_pkt_str, lag_pkt_list)
+    )
+    lag_row = [lag_hash_val, lag_algo_val, lag_pkt_val]
     lag_body.append(lag_row)
 
     body.append(["ECMP", tabulate.tabulate(ecmp_body, sub_header, "psql")])
diff --git a/tests/hash_input/assert_show_output.py b/tests/hash_input/assert_show_output.py
index d2aa9719..5c1a1dfc 100644
--- a/tests/hash_input/assert_show_output.py
+++ b/tests/hash_input/assert_show_output.py
@@ -233,129 +233,135 @@ show_hash_ecmp_and_lag_json="""\
 """
 
 show_hash_capabilities_no="""\
-+--------+---------------------------------------+
-| Hash   | Capabilities                          |
-+========+=======================================+
-| ECMP   | +-----------------+-----------------+ |
-|        | | Hash Field      | Algorithm       | |
-|        | |-----------------+-----------------| |
-|        | | no capabilities | no capabilities | |
-|        | +-----------------+-----------------+ |
-+--------+---------------------------------------+
-| LAG    | +-----------------+-----------------+ |
-|        | | Hash Field      | Algorithm       | |
-|        | |-----------------+-----------------| |
-|        | | no capabilities | no capabilities | |
-|        | +-----------------+-----------------+ |
-+--------+---------------------------------------+
++--------+---------------------------------------------------------+
+| Hash   | Capabilities                                            |
++========+=========================================================+
+| ECMP   | +-----------------+-----------------+-----------------+ |
+|        | | Hash Field      | Algorithm       | Packet Type     | |
+|        | |-----------------+-----------------+-----------------| |
+|        | | no capabilities | no capabilities | no capabilities | |
+|        | +-----------------+-----------------+-----------------+ |
++--------+---------------------------------------------------------+
+| LAG    | +-----------------+-----------------+-----------------+ |
+|        | | Hash Field      | Algorithm       | Packet Type     | |
+|        | |-----------------+-----------------+-----------------| |
+|        | | no capabilities | no capabilities | no capabilities | |
+|        | +-----------------+-----------------+-----------------+ |
++--------+---------------------------------------------------------+
 """
 show_hash_capabilities_no_json="""\
 {
     "ecmp": {
         "hash_field": "no capabilities",
-        "algorithm": "no capabilities"
+        "algorithm": "no capabilities",
+        "pkt_type": "no capabilities"
     },
     "lag": {
         "hash_field": "no capabilities",
-        "algorithm": "no capabilities"
+        "algorithm": "no capabilities",
+        "pkt_type": "no capabilities"
     }
 }
 """
 
-show_hash_capabilities_na="""\
-+--------+--------------------------------+
-| Hash   | Capabilities                   |
-+========+================================+
-| ECMP   | +--------------+-------------+ |
-|        | | Hash Field   | Algorithm   | |
-|        | |--------------+-------------| |
-|        | | N/A          | N/A         | |
-|        | +--------------+-------------+ |
-+--------+--------------------------------+
-| LAG    | +--------------+-------------+ |
-|        | | Hash Field   | Algorithm   | |
-|        | |--------------+-------------| |
-|        | | N/A          | N/A         | |
-|        | +--------------+-------------+ |
-+--------+--------------------------------+
+show_hash_capabilities_na = """\
++--------+------------------------------------------------+
+| Hash   | Capabilities                                   |
++========+================================================+
+| ECMP   | +--------------+-------------+---------------+ |
+|        | | Hash Field   | Algorithm   | Packet Type   | |
+|        | |--------------+-------------+---------------| |
+|        | | N/A          | N/A         | N/A           | |
+|        | +--------------+-------------+---------------+ |
++--------+------------------------------------------------+
+| LAG    | +--------------+-------------+---------------+ |
+|        | | Hash Field   | Algorithm   | Packet Type   | |
+|        | |--------------+-------------+---------------| |
+|        | | N/A          | N/A         | N/A           | |
+|        | +--------------+-------------+---------------+ |
++--------+------------------------------------------------+
 """
 show_hash_capabilities_na_json="""\
 {
     "ecmp": {
         "hash_field": "N/A",
-        "algorithm": "N/A"
+        "algorithm": "N/A",
+        "pkt_type": "N/A"
     },
     "lag": {
         "hash_field": "N/A",
-        "algorithm": "N/A"
+        "algorithm": "N/A",
+        "pkt_type": "N/A"
     }
 }
 """
 
 show_hash_capabilities_empty="""\
-+--------+-----------------------------------+
-| Hash   | Capabilities                      |
-+========+===================================+
-| ECMP   | +---------------+---------------+ |
-|        | | Hash Field    | Algorithm     | |
-|        | |---------------+---------------| |
-|        | | not supported | not supported | |
-|        | +---------------+---------------+ |
-+--------+-----------------------------------+
-| LAG    | +---------------+---------------+ |
-|        | | Hash Field    | Algorithm     | |
-|        | |---------------+---------------| |
-|        | | not supported | not supported | |
-|        | +---------------+---------------+ |
-+--------+-----------------------------------+
++--------+---------------------------------------------------+
+| Hash   | Capabilities                                      |
++========+===================================================+
+| ECMP   | +---------------+---------------+---------------+ |
+|        | | Hash Field    | Algorithm     | Packet Type   | |
+|        | |---------------+---------------+---------------| |
+|        | | not supported | not supported | not supported | |
+|        | +---------------+---------------+---------------+ |
++--------+---------------------------------------------------+
+| LAG    | +---------------+---------------+---------------+ |
+|        | | Hash Field    | Algorithm     | Packet Type   | |
+|        | |---------------+---------------+---------------| |
+|        | | not supported | not supported | not supported | |
+|        | +---------------+---------------+---------------+ |
++--------+---------------------------------------------------+
 """
 show_hash_capabilities_empty_json="""\
 {
     "ecmp": {
         "hash_field": "not supported",
-        "algorithm": "not supported"
+        "algorithm": "not supported",
+        "pkt_type": "not supported"
     },
     "lag": {
         "hash_field": "not supported",
-        "algorithm": "not supported"
+        "algorithm": "not supported",
+        "pkt_type": "not supported"
     }
 }
 """
 
 show_hash_capabilities_ecmp="""\
-+--------+-------------------------------------+
-| Hash   | Capabilities                        |
-+========+=====================================+
-| ECMP   | +-------------------+-------------+ |
-|        | | Hash Field        | Algorithm   | |
-|        | |-------------------+-------------| |
-|        | | IN_PORT           | CRC         | |
-|        | | DST_MAC           | XOR         | |
-|        | | SRC_MAC           | RANDOM      | |
-|        | | ETHERTYPE         | CRC_32LO    | |
-|        | | VLAN_ID           | CRC_32HI    | |
-|        | | IP_PROTOCOL       | CRC_CCITT   | |
-|        | | DST_IP            | CRC_XOR     | |
-|        | | SRC_IP            |             | |
-|        | | L4_DST_PORT       |             | |
-|        | | L4_SRC_PORT       |             | |
-|        | | INNER_DST_MAC     |             | |
-|        | | INNER_SRC_MAC     |             | |
-|        | | INNER_ETHERTYPE   |             | |
-|        | | INNER_IP_PROTOCOL |             | |
-|        | | INNER_DST_IP      |             | |
-|        | | INNER_SRC_IP      |             | |
-|        | | INNER_L4_DST_PORT |             | |
-|        | | INNER_L4_SRC_PORT |             | |
-|        | | IPV6_FLOW_LABEL   |             | |
-|        | +-------------------+-------------+ |
-+--------+-------------------------------------+
-| LAG    | +---------------+---------------+   |
-|        | | Hash Field    | Algorithm     |   |
-|        | |---------------+---------------|   |
-|        | | not supported | not supported |   |
-|        | +---------------+---------------+   |
-+--------+-------------------------------------+
++--------+-----------------------------------------------------+
+| Hash   | Capabilities                                        |
++========+=====================================================+
+| ECMP   | +-------------------+-------------+---------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type   | |
+|        | |-------------------+-------------+---------------| |
+|        | | IN_PORT           | CRC         | not supported | |
+|        | | DST_MAC           | XOR         |               | |
+|        | | SRC_MAC           | RANDOM      |               | |
+|        | | ETHERTYPE         | CRC_32LO    |               | |
+|        | | VLAN_ID           | CRC_32HI    |               | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |               | |
+|        | | DST_IP            | CRC_XOR     |               | |
+|        | | SRC_IP            |             |               | |
+|        | | L4_DST_PORT       |             |               | |
+|        | | L4_SRC_PORT       |             |               | |
+|        | | INNER_DST_MAC     |             |               | |
+|        | | INNER_SRC_MAC     |             |               | |
+|        | | INNER_ETHERTYPE   |             |               | |
+|        | | INNER_IP_PROTOCOL |             |               | |
+|        | | INNER_DST_IP      |             |               | |
+|        | | INNER_SRC_IP      |             |               | |
+|        | | INNER_L4_DST_PORT |             |               | |
+|        | | INNER_L4_SRC_PORT |             |               | |
+|        | | IPV6_FLOW_LABEL   |             |               | |
+|        | +-------------------+-------------+---------------+ |
++--------+-----------------------------------------------------+
+| LAG    | +---------------+---------------+---------------+   |
+|        | | Hash Field    | Algorithm     | Packet Type   |   |
+|        | |---------------+---------------+---------------|   |
+|        | | not supported | not supported | not supported |   |
+|        | +---------------+---------------+---------------+   |
++--------+-----------------------------------------------------+
 """
 show_hash_capabilities_ecmp_json="""\
 {
@@ -389,55 +395,58 @@ show_hash_capabilities_ecmp_json="""\
             "CRC_32HI",
             "CRC_CCITT",
             "CRC_XOR"
-        ]
+        ],
+        "pkt_type": "not supported"
     },
     "lag": {
         "hash_field": "not supported",
-        "algorithm": "not supported"
+        "algorithm": "not supported",
+        "pkt_type": "not supported"
     }
 }
 """
 
 show_hash_capabilities_lag="""\
-+--------+-------------------------------------+
-| Hash   | Capabilities                        |
-+========+=====================================+
-| ECMP   | +---------------+---------------+   |
-|        | | Hash Field    | Algorithm     |   |
-|        | |---------------+---------------|   |
-|        | | not supported | not supported |   |
-|        | +---------------+---------------+   |
-+--------+-------------------------------------+
-| LAG    | +-------------------+-------------+ |
-|        | | Hash Field        | Algorithm   | |
-|        | |-------------------+-------------| |
-|        | | IN_PORT           | CRC         | |
-|        | | DST_MAC           | XOR         | |
-|        | | SRC_MAC           | RANDOM      | |
-|        | | ETHERTYPE         | CRC_32LO    | |
-|        | | VLAN_ID           | CRC_32HI    | |
-|        | | IP_PROTOCOL       | CRC_CCITT   | |
-|        | | DST_IP            | CRC_XOR     | |
-|        | | SRC_IP            |             | |
-|        | | L4_DST_PORT       |             | |
-|        | | L4_SRC_PORT       |             | |
-|        | | INNER_DST_MAC     |             | |
-|        | | INNER_SRC_MAC     |             | |
-|        | | INNER_ETHERTYPE   |             | |
-|        | | INNER_IP_PROTOCOL |             | |
-|        | | INNER_DST_IP      |             | |
-|        | | INNER_SRC_IP      |             | |
-|        | | INNER_L4_DST_PORT |             | |
-|        | | INNER_L4_SRC_PORT |             | |
-|        | | IPV6_FLOW_LABEL   |             | |
-|        | +-------------------+-------------+ |
-+--------+-------------------------------------+
++--------+-----------------------------------------------------+
+| Hash   | Capabilities                                        |
++========+=====================================================+
+| ECMP   | +---------------+---------------+---------------+   |
+|        | | Hash Field    | Algorithm     | Packet Type   |   |
+|        | |---------------+---------------+---------------|   |
+|        | | not supported | not supported | not supported |   |
+|        | +---------------+---------------+---------------+   |
++--------+-----------------------------------------------------+
+| LAG    | +-------------------+-------------+---------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type   | |
+|        | |-------------------+-------------+---------------| |
+|        | | IN_PORT           | CRC         | not supported | |
+|        | | DST_MAC           | XOR         |               | |
+|        | | SRC_MAC           | RANDOM      |               | |
+|        | | ETHERTYPE         | CRC_32LO    |               | |
+|        | | VLAN_ID           | CRC_32HI    |               | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |               | |
+|        | | DST_IP            | CRC_XOR     |               | |
+|        | | SRC_IP            |             |               | |
+|        | | L4_DST_PORT       |             |               | |
+|        | | L4_SRC_PORT       |             |               | |
+|        | | INNER_DST_MAC     |             |               | |
+|        | | INNER_SRC_MAC     |             |               | |
+|        | | INNER_ETHERTYPE   |             |               | |
+|        | | INNER_IP_PROTOCOL |             |               | |
+|        | | INNER_DST_IP      |             |               | |
+|        | | INNER_SRC_IP      |             |               | |
+|        | | INNER_L4_DST_PORT |             |               | |
+|        | | INNER_L4_SRC_PORT |             |               | |
+|        | | IPV6_FLOW_LABEL   |             |               | |
+|        | +-------------------+-------------+---------------+ |
++--------+-----------------------------------------------------+
 """
 show_hash_capabilities_lag_json="""\
 {
     "ecmp": {
         "hash_field": "not supported",
-        "algorithm": "not supported"
+        "algorithm": "not supported",
+        "pkt_type": "not supported"
     },
     "lag": {
         "hash_field": [
@@ -469,63 +478,68 @@ show_hash_capabilities_lag_json="""\
             "CRC_32HI",
             "CRC_CCITT",
             "CRC_XOR"
-        ]
+        ],
+        "pkt_type": "not supported"
     }
 }
 """
 
 show_hash_capabilities_ecmp_and_lag="""\
-+--------+-------------------------------------+
-| Hash   | Capabilities                        |
-+========+=====================================+
-| ECMP   | +-------------------+-------------+ |
-|        | | Hash Field        | Algorithm   | |
-|        | |-------------------+-------------| |
-|        | | IN_PORT           | CRC         | |
-|        | | DST_MAC           | XOR         | |
-|        | | SRC_MAC           | RANDOM      | |
-|        | | ETHERTYPE         | CRC_32LO    | |
-|        | | VLAN_ID           | CRC_32HI    | |
-|        | | IP_PROTOCOL       | CRC_CCITT   | |
-|        | | DST_IP            | CRC_XOR     | |
-|        | | SRC_IP            |             | |
-|        | | L4_DST_PORT       |             | |
-|        | | L4_SRC_PORT       |             | |
-|        | | INNER_DST_MAC     |             | |
-|        | | INNER_SRC_MAC     |             | |
-|        | | INNER_ETHERTYPE   |             | |
-|        | | INNER_IP_PROTOCOL |             | |
-|        | | INNER_DST_IP      |             | |
-|        | | INNER_SRC_IP      |             | |
-|        | | INNER_L4_DST_PORT |             | |
-|        | | INNER_L4_SRC_PORT |             | |
-|        | | IPV6_FLOW_LABEL   |             | |
-|        | +-------------------+-------------+ |
-+--------+-------------------------------------+
-| LAG    | +-------------------+-------------+ |
-|        | | Hash Field        | Algorithm   | |
-|        | |-------------------+-------------| |
-|        | | IN_PORT           | CRC         | |
-|        | | DST_MAC           | XOR         | |
-|        | | SRC_MAC           | RANDOM      | |
-|        | | ETHERTYPE         | CRC_32LO    | |
-|        | | VLAN_ID           | CRC_32HI    | |
-|        | | IP_PROTOCOL       | CRC_CCITT   | |
-|        | | DST_IP            | CRC_XOR     | |
-|        | | SRC_IP            |             | |
-|        | | L4_DST_PORT       |             | |
-|        | | L4_SRC_PORT       |             | |
-|        | | INNER_DST_MAC     |             | |
-|        | | INNER_SRC_MAC     |             | |
-|        | | INNER_ETHERTYPE   |             | |
-|        | | INNER_IP_PROTOCOL |             | |
-|        | | INNER_DST_IP      |             | |
-|        | | INNER_SRC_IP      |             | |
-|        | | INNER_L4_DST_PORT |             | |
-|        | | INNER_L4_SRC_PORT |             | |
-|        | | IPV6_FLOW_LABEL   |             | |
-|        | +-------------------+-------------+ |
-+--------+-------------------------------------+
++--------+-----------------------------------------------------+
+| Hash   | Capabilities                                        |
++========+=====================================================+
+| ECMP   | +-------------------+-------------+---------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type   | |
+|        | |-------------------+-------------+---------------| |
+|        | | IN_PORT           | CRC         | not supported | |
+|        | | DST_MAC           | XOR         |               | |
+|        | | SRC_MAC           | RANDOM      |               | |
+|        | | ETHERTYPE         | CRC_32LO    |               | |
+|        | | VLAN_ID           | CRC_32HI    |               | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |               | |
+|        | | DST_IP            | CRC_XOR     |               | |
+|        | | SRC_IP            |             |               | |
+|        | | L4_DST_PORT       |             |               | |
+|        | | L4_SRC_PORT       |             |               | |
+|        | | INNER_DST_MAC     |             |               | |
+|        | | INNER_SRC_MAC     |             |               | |
+|        | | INNER_ETHERTYPE   |             |               | |
+|        | | INNER_IP_PROTOCOL |             |               | |
+|        | | INNER_DST_IP      |             |               | |
+|        | | INNER_SRC_IP      |             |               | |
+|        | | INNER_L4_DST_PORT |             |               | |
+|        | | INNER_L4_SRC_PORT |             |               | |
+|        | | IPV6_FLOW_LABEL   |             |               | |
+|        | | RDMA_BTH_OPCODE   |             |               | |
+|        | | RDMA_BTH_DEST_QP  |             |               | |
+|        | +-------------------+-------------+---------------+ |
++--------+-----------------------------------------------------+
+| LAG    | +-------------------+-------------+---------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type   | |
+|        | |-------------------+-------------+---------------| |
+|        | | IN_PORT           | CRC         | not supported | |
+|        | | DST_MAC           | XOR         |               | |
+|        | | SRC_MAC           | RANDOM      |               | |
+|        | | ETHERTYPE         | CRC_32LO    |               | |
+|        | | VLAN_ID           | CRC_32HI    |               | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |               | |
+|        | | DST_IP            | CRC_XOR     |               | |
+|        | | SRC_IP            |             |               | |
+|        | | L4_DST_PORT       |             |               | |
+|        | | L4_SRC_PORT       |             |               | |
+|        | | INNER_DST_MAC     |             |               | |
+|        | | INNER_SRC_MAC     |             |               | |
+|        | | INNER_ETHERTYPE   |             |               | |
+|        | | INNER_IP_PROTOCOL |             |               | |
+|        | | INNER_DST_IP      |             |               | |
+|        | | INNER_SRC_IP      |             |               | |
+|        | | INNER_L4_DST_PORT |             |               | |
+|        | | INNER_L4_SRC_PORT |             |               | |
+|        | | IPV6_FLOW_LABEL   |             |               | |
+|        | | RDMA_BTH_OPCODE   |             |               | |
+|        | | RDMA_BTH_DEST_QP  |             |               | |
+|        | +-------------------+-------------+---------------+ |
++--------+-----------------------------------------------------+
 """
 show_hash_capabilities_ecmp_and_lag_json="""\
 {
@@ -549,7 +563,44 @@ show_hash_capabilities_ecmp_and_lag_json="""\
             "INNER_SRC_IP",
             "INNER_L4_DST_PORT",
             "INNER_L4_SRC_PORT",
-            "IPV6_FLOW_LABEL"
+            "IPV6_FLOW_LABEL",
+            "RDMA_BTH_OPCODE",
+            "RDMA_BTH_DEST_QP"
+        ],
+        "algorithm": [
+            "CRC",
+            "XOR",
+            "RANDOM",
+            "CRC_32LO",
+            "CRC_32HI",
+            "CRC_CCITT",
+            "CRC_XOR"
+        ],
+        "pkt_type": "not supported"
+    },
+    "lag": {
+        "hash_field": [
+            "IN_PORT",
+            "DST_MAC",
+            "SRC_MAC",
+            "ETHERTYPE",
+            "VLAN_ID",
+            "IP_PROTOCOL",
+            "DST_IP",
+            "SRC_IP",
+            "L4_DST_PORT",
+            "L4_SRC_PORT",
+            "INNER_DST_MAC",
+            "INNER_SRC_MAC",
+            "INNER_ETHERTYPE",
+            "INNER_IP_PROTOCOL",
+            "INNER_DST_IP",
+            "INNER_SRC_IP",
+            "INNER_L4_DST_PORT",
+            "INNER_L4_SRC_PORT",
+            "IPV6_FLOW_LABEL",
+            "RDMA_BTH_OPCODE",
+            "RDMA_BTH_DEST_QP"
         ],
         "algorithm": [
             "CRC",
@@ -559,6 +610,109 @@ show_hash_capabilities_ecmp_and_lag_json="""\
             "CRC_32HI",
             "CRC_CCITT",
             "CRC_XOR"
+        ],
+        "pkt_type": "not supported"
+    }
+}
+"""
+show_hash_capabilities_pkttype_ecmp_and_lag="""\
++--------+---------------------------------------------------+
+| Hash   | Capabilities                                      |
++========+===================================================+
+| ECMP   | +-------------------+-------------+-------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type | |
+|        | |-------------------+-------------+-------------| |
+|        | | IN_PORT           | CRC         | ipv4        | |
+|        | | DST_MAC           | XOR         | ipv4        | |
+|        | | SRC_MAC           | RANDOM      | ipnip       | |
+|        | | ETHERTYPE         | CRC_32LO    | ipv4_rdma   | |
+|        | | VLAN_ID           | CRC_32HI    | ipv6_rdma   | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |             | |
+|        | | DST_IP            | CRC_XOR     |             | |
+|        | | SRC_IP            |             |             | |
+|        | | L4_DST_PORT       |             |             | |
+|        | | L4_SRC_PORT       |             |             | |
+|        | | INNER_DST_MAC     |             |             | |
+|        | | INNER_SRC_MAC     |             |             | |
+|        | | INNER_ETHERTYPE   |             |             | |
+|        | | INNER_IP_PROTOCOL |             |             | |
+|        | | INNER_DST_IP      |             |             | |
+|        | | INNER_SRC_IP      |             |             | |
+|        | | INNER_L4_DST_PORT |             |             | |
+|        | | INNER_L4_SRC_PORT |             |             | |
+|        | | IPV6_FLOW_LABEL   |             |             | |
+|        | | RDMA_BTH_OPCODE   |             |             | |
+|        | | RDMA_BTH_DEST_QP  |             |             | |
+|        | +-------------------+-------------+-------------+ |
++--------+---------------------------------------------------+
+| LAG    | +-------------------+-------------+-------------+ |
+|        | | Hash Field        | Algorithm   | Packet Type | |
+|        | |-------------------+-------------+-------------| |
+|        | | IN_PORT           | CRC         | ipv4        | |
+|        | | DST_MAC           | XOR         | ipv4        | |
+|        | | SRC_MAC           | RANDOM      | ipnip       | |
+|        | | ETHERTYPE         | CRC_32LO    | ipv4_rdma   | |
+|        | | VLAN_ID           | CRC_32HI    | ipv6_rdma   | |
+|        | | IP_PROTOCOL       | CRC_CCITT   |             | |
+|        | | DST_IP            | CRC_XOR     |             | |
+|        | | SRC_IP            |             |             | |
+|        | | L4_DST_PORT       |             |             | |
+|        | | L4_SRC_PORT       |             |             | |
+|        | | INNER_DST_MAC     |             |             | |
+|        | | INNER_SRC_MAC     |             |             | |
+|        | | INNER_ETHERTYPE   |             |             | |
+|        | | INNER_IP_PROTOCOL |             |             | |
+|        | | INNER_DST_IP      |             |             | |
+|        | | INNER_SRC_IP      |             |             | |
+|        | | INNER_L4_DST_PORT |             |             | |
+|        | | INNER_L4_SRC_PORT |             |             | |
+|        | | IPV6_FLOW_LABEL   |             |             | |
+|        | | RDMA_BTH_OPCODE   |             |             | |
+|        | | RDMA_BTH_DEST_QP  |             |             | |
+|        | +-------------------+-------------+-------------+ |
++--------+---------------------------------------------------+
+"""
+show_hash_capabilities_pkttype_ecmp_and_lag_json="""\
+{
+    "ecmp": {
+        "hash_field": [
+            "IN_PORT",
+            "DST_MAC",
+            "SRC_MAC",
+            "ETHERTYPE",
+            "VLAN_ID",
+            "IP_PROTOCOL",
+            "DST_IP",
+            "SRC_IP",
+            "L4_DST_PORT",
+            "L4_SRC_PORT",
+            "INNER_DST_MAC",
+            "INNER_SRC_MAC",
+            "INNER_ETHERTYPE",
+            "INNER_IP_PROTOCOL",
+            "INNER_DST_IP",
+            "INNER_SRC_IP",
+            "INNER_L4_DST_PORT",
+            "INNER_L4_SRC_PORT",
+            "IPV6_FLOW_LABEL",
+            "RDMA_BTH_OPCODE",
+            "RDMA_BTH_DEST_QP"
+        ],
+        "algorithm": [
+            "CRC",
+            "XOR",
+            "RANDOM",
+            "CRC_32LO",
+            "CRC_32HI",
+            "CRC_CCITT",
+            "CRC_XOR"
+        ],
+        "pkt_type": [
+            "ipv4",
+            "ipv6",
+            "ipnip",
+            "ipv4_rdma",
+            "ipv6_rdma"
         ]
     },
     "lag": {
@@ -581,7 +735,9 @@ show_hash_capabilities_ecmp_and_lag_json="""\
             "INNER_SRC_IP",
             "INNER_L4_DST_PORT",
             "INNER_L4_SRC_PORT",
-            "IPV6_FLOW_LABEL"
+            "IPV6_FLOW_LABEL",
+            "RDMA_BTH_OPCODE",
+            "RDMA_BTH_DEST_QP"
         ],
         "algorithm": [
             "CRC",
@@ -591,6 +747,13 @@ show_hash_capabilities_ecmp_and_lag_json="""\
             "CRC_32HI",
             "CRC_CCITT",
             "CRC_XOR"
+        ],
+        "pkt_type": [
+            "ipv4",
+            "ipv6",
+            "ipnip",
+            "ipv4_rdma",
+            "ipv6_rdma"
         ]
     }
 }
diff --git a/tests/hash_input/mock_state/ecmp.json b/tests/hash_input/mock_state/ecmp.json
index a4b81a9e..b3abf17a 100644
--- a/tests/hash_input/mock_state/ecmp.json
+++ b/tests/hash_input/mock_state/ecmp.json
@@ -6,6 +6,10 @@
         "LAG_HASH_ALGORITHM_CAPABLE": "false",
         "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL",
         "ECMP_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
-        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR"
+        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "false",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "false",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
     }
 }
diff --git a/tests/hash_input/mock_state/ecmp_and_lag.json b/tests/hash_input/mock_state/ecmp_and_lag.json
index f64ab343..dbadef9d 100644
--- a/tests/hash_input/mock_state/ecmp_and_lag.json
+++ b/tests/hash_input/mock_state/ecmp_and_lag.json
@@ -4,8 +4,12 @@
         "LAG_HASH_CAPABLE": "true",
         "ECMP_HASH_ALGORITHM_CAPABLE": "true",
         "LAG_HASH_ALGORITHM_CAPABLE": "true",
-        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL",
+        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL,RDMA_BTH_OPCODE,RDMA_BTH_DEST_QP",
         "ECMP_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
-        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR"
+        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "false",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "false",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
     }
 }
diff --git a/tests/hash_input/mock_state/empty.json b/tests/hash_input/mock_state/empty.json
index 014f4400..c0455d65 100644
--- a/tests/hash_input/mock_state/empty.json
+++ b/tests/hash_input/mock_state/empty.json
@@ -6,6 +6,10 @@
         "LAG_HASH_ALGORITHM_CAPABLE": "false",
         "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL",
         "ECMP_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
-        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR"
+        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "false",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "false",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
     }
 }
diff --git a/tests/hash_input/mock_state/lag.json b/tests/hash_input/mock_state/lag.json
index 7fa464b7..53fa7ca9 100644
--- a/tests/hash_input/mock_state/lag.json
+++ b/tests/hash_input/mock_state/lag.json
@@ -6,6 +6,10 @@
         "LAG_HASH_ALGORITHM_CAPABLE": "true",
         "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL",
         "ECMP_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
-        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR"
+        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "false",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "false",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
     }
 }
diff --git a/tests/hash_input/mock_state/no_capabilities.json b/tests/hash_input/mock_state/no_capabilities.json
index 5e4652cc..40c8fece 100644
--- a/tests/hash_input/mock_state/no_capabilities.json
+++ b/tests/hash_input/mock_state/no_capabilities.json
@@ -6,6 +6,10 @@
         "LAG_HASH_ALGORITHM_CAPABLE": "true",
         "HASH|NATIVE_HASH_FIELD_LIST": "",
         "ECMP_HASH_ALGORITHM": "",
-        "LAG_HASH_ALGORITHM": ""
+        "LAG_HASH_ALGORITHM": "",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "true",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "true",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
     }
 }
diff --git a/tests/hash_input/mock_state/not_applicable.json b/tests/hash_input/mock_state/not_applicable.json
index 6495b585..6f6b2962 100644
--- a/tests/hash_input/mock_state/not_applicable.json
+++ b/tests/hash_input/mock_state/not_applicable.json
@@ -6,6 +6,11 @@
         "LAG_HASH_ALGORITHM_CAPABLE": "true",
         "HASH|NATIVE_HASH_FIELD_LIST": "N/A",
         "ECMP_HASH_ALGORITHM": "N/A",
-        "LAG_HASH_ALGORITHM": "N/A"
+        "LAG_HASH_ALGORITHM": "N/A",
+
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "true",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "true",
+        "HASH|ECMP_PKT_TYPE_LIST": "N/A",
+        "HASH|LAG_PKT_TYPE_LIST": "N/A"
     }
 }
diff --git a/tests/hash_input/mock_state/pkttype_disabled.json b/tests/hash_input/mock_state/pkttype_disabled.json
new file mode 100644
index 00000000..9a4a462a
--- /dev/null
+++ b/tests/hash_input/mock_state/pkttype_disabled.json
@@ -0,0 +1,15 @@
+{
+    "SWITCH_CAPABILITY|switch": {
+        "ECMP_HASH_CAPABLE": "true",
+        "LAG_HASH_CAPABLE": "true",
+        "ECMP_HASH_ALGORITHM_CAPABLE": "true",
+        "LAG_HASH_ALGORITHM_CAPABLE": "true",
+
+        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL",
+
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "false",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "false",
+        "HASH|ECMP_PKT_TYPE_LIST": "",
+        "HASH|LAG_PKT_TYPE_LIST": ""
+    }
+}
diff --git a/tests/hash_input/mock_state/pkttype_ecmp_and_lag.json b/tests/hash_input/mock_state/pkttype_ecmp_and_lag.json
new file mode 100644
index 00000000..63295c2f
--- /dev/null
+++ b/tests/hash_input/mock_state/pkttype_ecmp_and_lag.json
@@ -0,0 +1,15 @@
+{
+    "SWITCH_CAPABILITY|switch": {
+        "ECMP_HASH_CAPABLE": "true",
+        "LAG_HASH_CAPABLE": "true",
+        "ECMP_HASH_ALGORITHM_CAPABLE": "true",
+        "LAG_HASH_ALGORITHM_CAPABLE": "true",
+        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL,RDMA_BTH_OPCODE,RDMA_BTH_DEST_QP",
+        "ECMP_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "LAG_HASH_ALGORITHM": "CRC,XOR,RANDOM,CRC_32LO,CRC_32HI,CRC_CCITT,CRC_XOR",
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "true",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "true",
+        "HASH|ECMP_PKT_TYPE_LIST": "IPV4,IPV6,IPNIP,IPV4_RDMA,IPV6_RDMA",
+        "HASH|LAG_PKT_TYPE_LIST": "IPV4,IPV6,IPNIP,IPV4_RDMA,IPV6_RDMA"
+    }
+}
diff --git a/tests/hash_input/mock_state/pkttype_limited.json b/tests/hash_input/mock_state/pkttype_limited.json
new file mode 100644
index 00000000..ba1397e1
--- /dev/null
+++ b/tests/hash_input/mock_state/pkttype_limited.json
@@ -0,0 +1,15 @@
+{
+    "SWITCH_CAPABILITY|switch": {
+        "ECMP_HASH_CAPABLE": "true",
+        "LAG_HASH_CAPABLE": "true",
+        "ECMP_HASH_ALGORITHM_CAPABLE": "true",
+        "LAG_HASH_ALGORITHM_CAPABLE": "true",
+
+        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,INNER_DST_MAC,INNER_SRC_MAC,INNER_ETHERTYPE,INNER_IP_PROTOCOL,INNER_DST_IP,INNER_SRC_IP,INNER_L4_DST_PORT,INNER_L4_SRC_PORT,IPV6_FLOW_LABEL,RDMA_BTH_OPCODE,RDMA_BTH_DEST_QP",
+
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "true",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "true",
+        "HASH|ECMP_PKT_TYPE_LIST": "IPV4,IPV6",
+        "HASH|LAG_PKT_TYPE_LIST": "IPV4,IPV6"
+    }
+}
diff --git a/tests/hash_input/mock_state/pkttype_limited_fields.json b/tests/hash_input/mock_state/pkttype_limited_fields.json
new file mode 100644
index 00000000..07036cd1
--- /dev/null
+++ b/tests/hash_input/mock_state/pkttype_limited_fields.json
@@ -0,0 +1,15 @@
+{
+    "SWITCH_CAPABILITY|switch": {
+        "ECMP_HASH_CAPABLE": "true",
+        "LAG_HASH_CAPABLE": "true",
+        "ECMP_HASH_ALGORITHM_CAPABLE": "true",
+        "LAG_HASH_ALGORITHM_CAPABLE": "true",
+
+        "HASH|NATIVE_HASH_FIELD_LIST": "IN_PORT,DST_MAC,SRC_MAC,ETHERTYPE,VLAN_ID,IP_PROTOCOL,DST_IP,SRC_IP,L4_DST_PORT,L4_SRC_PORT,RDMA_BTH_OPCODE",
+
+        "ECMP_PKT_TYPE_HASH_CAPABLE": "true",
+        "LAG_PKT_TYPE_HASH_CAPABLE": "true",
+        "HASH|ECMP_PKT_TYPE_LIST": "IPV4_RDMA",
+        "HASH|LAG_PKT_TYPE_LIST": "IPV4_RDMA"
+    }
+}
diff --git a/tests/hash_test.py b/tests/hash_test.py
index 25fcf785..83709488 100644
--- a/tests/hash_test.py
+++ b/tests/hash_test.py
@@ -4,6 +4,7 @@ import logging
 import show.main as show
 import config.main as config
 
+
 from click.testing import CliRunner
 from utilities_common.db import Db
 from .mock_tables import dbconnector
@@ -15,6 +16,8 @@ input_path = os.path.join(test_path, "hash_input")
 mock_config_path = os.path.join(input_path, "mock_config")
 mock_state_path = os.path.join(input_path, "mock_state")
 
+PKTTYPE_STATE_DB = os.path.join(mock_state_path, "pkttype_ecmp_and_lag")
+
 logger = logging.getLogger(__name__)
 
 
@@ -49,6 +52,27 @@ HASH_ALGORITHM = [
     "CRC_XOR"
 ]
 
+PKT_TYPE_LIST = [
+    "ipv4",
+    "ipv6",
+    "ipnip",
+    "ipv4-rdma",
+    "ipv6-rdma"
+]
+
+RDMA_HASH_FIELD_LIST = [
+    "DST_MAC",
+    "SRC_MAC",
+    "ETHERTYPE",
+    "IP_PROTOCOL",
+    "DST_IP",
+    "SRC_IP",
+    "L4_DST_PORT",
+    "L4_SRC_PORT",
+    "RDMA_BTH_OPCODE",
+    "RDMA_BTH_DEST_QP"
+]
+
 SUCCESS = 0
 ERROR2 = 2
 
@@ -93,8 +117,8 @@ class TestHash:
         runner = CliRunner()
 
         result = runner.invoke(
-            config.config.commands["switch-hash"].commands["global"].
-            commands[hash], args, obj=db
+            config.config.commands["switch-hash"].commands["global"].commands[hash],
+            args, obj=db
         )
 
         logger.debug("\n" + result.output)
@@ -147,8 +171,8 @@ class TestHash:
         runner = CliRunner()
 
         result = runner.invoke(
-            config.config.commands["switch-hash"].commands["global"].
-            commands[hash], args, obj=db
+            config.config.commands["switch-hash"].commands["global"].commands[hash],
+            args, obj=db
         )
 
         logger.debug("\n" + result.output)
@@ -171,8 +195,8 @@ class TestHash:
         runner = CliRunner()
 
         result = runner.invoke(
-            config.config.commands["switch-hash"].commands["global"].
-            commands[hash], arg, obj=db
+            config.config.commands["switch-hash"].commands["global"].commands[hash],
+            arg, obj=db
         )
 
         logger.debug("\n" + result.output)
@@ -200,8 +224,8 @@ class TestHash:
         runner = CliRunner()
 
         result = runner.invoke(
-            config.config.commands["switch-hash"].commands["global"].
-            commands[hash], arg, obj=db
+            config.config.commands["switch-hash"].commands["global"].commands[hash],
+            arg, obj=db
         )
 
         logger.debug("\n" + result.output)
@@ -347,3 +371,419 @@ class TestHash:
 
         assert result.output == output[format]
         assert result.exit_code == SUCCESS
+
+    ########## CONFIG PKT-TYPE SWITCH HASH ######
+    @pytest.mark.parametrize(
+        "hash_cmd", [
+            "ecmp-hash",
+            "lag-hash"
+        ]
+    )
+    @pytest.mark.parametrize(
+        "pkt_type", PKT_TYPE_LIST
+    )
+    @pytest.mark.parametrize(
+        "action", [
+            "add",
+            "del"
+        ]
+    )
+    @pytest.mark.parametrize(
+        "args", [
+            pytest.param(
+                " ".join(RDMA_HASH_FIELD_LIST),
+                id="pkt-type"
+            )
+        ]
+    )
+    def test_config_hash_packet_type(self, hash_cmd, pkt_type, action, args):
+        """Test packet-type add/del for ecmp-hash and lag-hash."""
+        # use pkt-type capable STATE_DB
+        dbconnector.dedicated_dbs["STATE_DB"] = PKTTYPE_STATE_DB
+
+        db = Db()
+        runner = CliRunner()
+
+        hash_fields = args.split()
+        # Build command arguments using --packet-type and --action options
+        cmd_args = ["--packet-type", pkt_type, "--action", action] + hash_fields
+
+        # Invoke the ecmp-hash command; packet-type is handled as an option
+        result = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            cmd_args,
+            obj=db
+        )
+        logger.debug(f"\nCommand: {hash_cmd} {cmd_args}")
+        logger.debug(f"Output: {result.output}")
+        logger.debug(f"Exit code: {result.exit_code}")
+
+        assert result.exit_code == SUCCESS
+
+    @pytest.mark.parametrize("hash_cmd, attr_prefix", [
+        pytest.param("ecmp-hash", "ecmp_hash_", id="ecmp"),
+        pytest.param("lag-hash",  "lag_hash_",  id="lag"),
+    ])
+    @pytest.mark.parametrize("pkt_type", PKT_TYPE_LIST)
+    def test_config_hash_pkt_type_add_incremental(self, hash_cmd, attr_prefix, pkt_type):
+        """
+        Incremental packet-type add behavior:
+
+        a) Add base fields: DST_MAC, SRC_MAC, ETHERTYPE
+        b) Add IP_PROTOCOL -> it should be appended to the existing list
+        c) Add existing field again (SRC_MAC) -> command should succeed,
+           but CONFIG_DB should not change (no duplicate, idempotent).
+        """
+        # use pkt-type capable STATE_DB
+        dbconnector.dedicated_dbs["STATE_DB"] = PKTTYPE_STATE_DB
+
+        db = Db()
+        runner = CliRunner()
+
+        base_fields = ["DST_MAC", "SRC_MAC", "ETHERTYPE"]
+        extra_field = "IP_PROTOCOL"
+        duplicate_field = "SRC_MAC"
+
+        # 1) Initial add
+        add1_cmd = ["--packet-type", pkt_type, "--action", "add"] + base_fields
+        res1 = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            add1_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nADD1 Command: {hash_cmd} {add1_cmd}")
+        logger.debug(f"Output: {res1.output}")
+        logger.debug(f"Exit code: {res1.exit_code}")
+        assert res1.exit_code == SUCCESS
+
+        # 2) Add an extra field (should append only if missing)
+        add2_cmd = ["--packet-type", pkt_type, "--action", "add", extra_field]
+        res2 = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            add2_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nADD2 Command: {hash_cmd} {add2_cmd}")
+        logger.debug(f"Output: {res2.output}")
+        logger.debug(f"Exit code: {res2.exit_code}")
+        assert res2.exit_code == SUCCESS
+
+        # Snapshot CONFIG_DB after ADD2
+        global_entry_after_add2 = db.cfgdb.get_entry("SWITCH_HASH", "GLOBAL")
+        # Normalize pkt_type for CONFIG_DB key: lowercase with '-' -> '_'
+        pkt_key = pkt_type.lower().replace("-", "_")
+        attr_name = f"{attr_prefix}{pkt_key}"
+        logger.debug(f"Looking for attribute: {attr_name}")
+        logger.debug(f"CONFIG_DB GLOBAL entry: {global_entry_after_add2}")
+        value_after_add2 = global_entry_after_add2.get(attr_name, "")
+        logger.debug(f"Raw value from CONFIG_DB: '{value_after_add2}'")
+        fields_after_add2 = value_after_add2.split(",") if value_after_add2 else []
+        logger.debug(f"Parsed fields: {fields_after_add2}")
+
+        # Expect base fields plus extra_field, no duplicates, order preserved
+        expected_after_add2 = base_fields + [extra_field]
+        assert fields_after_add2 == expected_after_add2
+
+        # 3) Re-add an existing field (duplicate); should succeed but not change CONFIG_DB
+        add3_cmd = ["--packet-type", pkt_type, "--action", "add", duplicate_field]
+        res3 = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            add3_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nADD3 Command: {hash_cmd} {add3_cmd}")
+        logger.debug(f"Output: {res3.output}")
+        logger.debug(f"Exit code: {res3.exit_code}")
+        assert res3.exit_code == SUCCESS
+
+        global_entry_after_add3 = db.cfgdb.get_entry("SWITCH_HASH", "GLOBAL")
+        value_after_add3 = global_entry_after_add3.get(attr_name, "")
+        fields_after_add3 = value_after_add3.split(",") if value_after_add3 else []
+
+        # No-op: CONFIG_DB should remain identical to state after ADD2
+        assert fields_after_add3 == fields_after_add2
+
+    @pytest.mark.parametrize(
+        "hash_cmd, attr_prefix",
+        [
+            pytest.param("ecmp-hash", "ecmp_hash_", id="ecmp"),
+            pytest.param("lag-hash",  "lag_hash_",  id="lag"),
+        ],
+    )
+    @pytest.mark.parametrize(
+        "pkt_type", PKT_TYPE_LIST
+    )
+    @pytest.mark.parametrize(
+        "delete_fields", [
+            pytest.param(
+                [],  # no args -> delete whole entry
+                id="del-no-args-delete-entry"
+            ),
+            pytest.param(
+                ["DST_MAC", "SRC_MAC"],  # delete subset only
+                id="del-specific-fields"
+            ),
+        ]
+    )
+    def test_config_hash_pkt_type_del_variants(self, hash_cmd, attr_prefix, pkt_type, delete_fields):
+        """packet-type del: no args deletes entry, args delete only specific fields (ECMP & LAG)."""
+        # use pkt-type capable STATE_DB
+        dbconnector.dedicated_dbs["STATE_DB"] = PKTTYPE_STATE_DB
+
+        db = Db()
+        runner = CliRunner()
+
+        # 1) Add full set of fields first
+        add_args = " ".join(HASH_FIELD_LIST)
+        add_cmd = ["--packet-type", pkt_type, "--action", "add"] + add_args.split()
+        add_result = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            add_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nADD Command: {hash_cmd} {add_cmd}")
+        logger.debug(f"Output: {add_result.output}")
+        logger.debug(f"Exit code: {add_result.exit_code}")
+        assert add_result.exit_code == SUCCESS
+
+        # 2) Run delete variant
+        del_cmd = ["--packet-type", pkt_type, "--action", "del"] + delete_fields
+        del_result = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            del_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nDEL Command: {hash_cmd} {del_cmd}")
+        logger.debug(f"Output: {del_result.output}")
+        logger.debug(f"Exit code: {del_result.exit_code}")
+        assert del_result.exit_code == SUCCESS
+
+        # 3) Validate CONFIG_DB state (GLOBAL row with per-pkt-type attr)
+        global_entry = db.cfgdb.get_entry("SWITCH_HASH", "GLOBAL")
+        # Normalize pkt_type for CONFIG_DB key: lowercase with '-' -> '_'
+        pkt_key = pkt_type.lower().replace("-", "_")
+        attr_name = f"{attr_prefix}{pkt_key}"
+        logger.debug(
+            f"GLOBAL entry after delete for {hash_cmd}, {pkt_type}, "
+            f"delete_fields={delete_fields}: {global_entry}"
+        )
+
+        if not delete_fields:
+            # No ARGS -> entire packet-type attribute should be gone
+            assert attr_name not in global_entry or not global_entry.get(attr_name)
+            return
+
+        value = global_entry.get(attr_name)
+        if not value:
+            # If attribute is gone here, it means delete_fields covered all fields
+            assert set(delete_fields) == set(HASH_FIELD_LIST)
+            return
+
+        remaining = value
+        if isinstance(remaining, str):
+            remaining = remaining.split(",")
+
+        # Ensure deleted fields are not present
+        for f in delete_fields:
+            assert f not in remaining
+
+        # Ensure other original fields remain
+        for f in HASH_FIELD_LIST:
+            if f not in delete_fields:
+                assert f in remaining
+
+    @pytest.mark.parametrize(
+        "pkt_type", PKT_TYPE_LIST
+    )
+    @pytest.mark.parametrize(
+        "cmd_suffix, pattern",
+        [
+            pytest.param(
+                ["bad_action"],  # invalid action
+                "'bad_action' is not one of",
+                id="invalid-action",
+            ),
+            pytest.param(
+                ["add"],  # add with no ARGS
+                "Hash fields are required when --packet-type is specified and --action is not 'del'",
+                id="add-no-args",
+            ),
+            pytest.param(
+                # 1) duplicate fields in packet-type mode
+                ["add", "DST_MAC", "DST_MAC", "SRC_MAC", "ETHERTYPE"],
+                "duplicate hash field(s) DST_MAC",
+                id="pkt-type-duplicate-fields",
+            ),
+            pytest.param(
+                # 2) invalid field in packet-type mode
+                ["add", "DST_MAC1", "SRC_MAC", "ETHERTYPE"],
+                "'DST_MAC1' is not one of",
+                id="pkt-type-invalid-field",
+            ),
+        ],
+    )
+    @pytest.mark.parametrize(
+        "hash_cmd",
+        [
+            "ecmp-hash",
+            "lag-hash",
+        ],
+    )
+    def test_config_hash_pkt_type_negative(self, hash_cmd, pkt_type, cmd_suffix, pattern):
+        """
+        Negative packet-type tests:
+          - invalid action word
+          - add with no HASH_FIELDS
+          - duplicate fields in packet-type mode
+          - invalid hash field in packet-type mode
+        """
+        # use pkt-type capable STATE_DB
+        dbconnector.dedicated_dbs["STATE_DB"] = PKTTYPE_STATE_DB
+
+        db = Db()
+        runner = CliRunner()
+
+        # Build command args based on the negative test case
+        if cmd_suffix[0] == "bad_action":
+            # Test invalid action
+            cmd_args = ["--packet-type", pkt_type, "--action", "bad_action"]
+        elif cmd_suffix[0] == "add" and len(cmd_suffix) == 1:
+            # Test add with no args
+            cmd_args = ["--packet-type", pkt_type, "--action", "add"]
+        else:
+            # Test with hash fields (duplicate or invalid)
+            action = cmd_suffix[0]
+            fields = cmd_suffix[1:]
+            cmd_args = ["--packet-type", pkt_type, "--action", action] + fields
+
+        result = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            cmd_args,
+            obj=db,
+        )
+
+        logger.debug(f"\nCommand: {hash_cmd} {cmd_args}")
+        logger.debug(f"Output: {result.output}")
+        logger.debug(f"Exit code: {result.exit_code}")
+
+        assert pattern in result.output
+        assert result.exit_code == ERROR2
+
+    @pytest.mark.parametrize(
+        "state_db_file, hash_cmd, pkt_type, args, expected_pattern",
+        [
+            # 1) pkt-type capability disabled
+            pytest.param(
+                os.path.join(mock_state_path, "pkttype_disabled"),
+                "ecmp-hash",
+                "ipv4",
+                " ".join(RDMA_HASH_FIELD_LIST),
+                "packet-type operation is not supported",
+                id="pkt-type-capability-disabled",
+            ),
+            # 2) pkt-type not in supported list
+            pytest.param(
+                os.path.join(mock_state_path, "pkttype_limited"),
+                "ecmp-hash",
+                "ipnip",
+                " ".join(RDMA_HASH_FIELD_LIST),
+                "packet-type IPNIP is not supported",
+                id="pkt-type-not-in-list",
+            ),
+            # 3) field not supported by HASH_FIELD_LIST in pkt-type mode
+            pytest.param(
+                os.path.join(mock_state_path, "pkttype_limited_fields"),
+                "ecmp-hash",
+                "ipv4-rdma",
+                " ".join(RDMA_HASH_FIELD_LIST),
+                "'RDMA_BTH_DEST_QP' is not one of",
+                id="pkt-type-field-not-supported",
+            ),
+        ],
+    )
+    def test_config_hash_pkt_type_capability_validation(
+        self, state_db_file, hash_cmd, pkt_type, args, expected_pattern
+    ):
+        """
+        Extra negative coverage for packet-type config:
+
+        1) pkt-type capability disabled
+        2) pkt-type not present in capability list
+        3) field not allowed by HASH_FIELD_LIST in pkt-type mode
+        """
+        dbconnector.dedicated_dbs["STATE_DB"] = state_db_file
+
+        db = Db()
+        runner = CliRunner()
+
+        cmd_args = ["--packet-type", pkt_type, "--action", "add"] + args.split()
+
+        result = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            cmd_args,
+            obj=db,
+        )
+
+        logger.debug(f"\nCommand: {hash_cmd} {cmd_args}")
+        logger.debug(f"Output: {result.output}")
+        logger.debug(f"Exit code: {result.exit_code}")
+
+        assert expected_pattern in result.output
+        assert result.exit_code == ERROR2
+
+    @pytest.mark.parametrize("hash_cmd, attr_prefix", [
+        pytest.param("ecmp-hash", "ecmp_hash_", id="ecmp"),
+        pytest.param("lag-hash",  "lag_hash_",  id="lag"),
+    ])
+    @pytest.mark.parametrize("pkt_type", PKT_TYPE_LIST)
+    def test_config_hash_pkt_type_overwrite_without_action(self, hash_cmd, attr_prefix, pkt_type):
+        """When --packet-type is given without --action, hash fields overwrite existing list."""
+        # use pkt-type capable STATE_DB
+        dbconnector.dedicated_dbs["STATE_DB"] = PKTTYPE_STATE_DB
+
+        db = Db()
+        runner = CliRunner()
+
+        # Initial list configured via explicit add (merge semantics)
+        initial_fields = ["DST_MAC", "SRC_MAC"]
+        add_cmd = ["--packet-type", pkt_type, "--action", "add"] + initial_fields
+        res_add = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            add_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nInitial ADD Command: {hash_cmd} {add_cmd}")
+        logger.debug(f"Output: {res_add.output}")
+        logger.debug(f"Exit code: {res_add.exit_code}")
+        assert res_add.exit_code == SUCCESS
+
+        # Overwrite list using --packet-type without --action
+        overwrite_fields = ["ETHERTYPE", "IP_PROTOCOL"]
+        overwrite_cmd = ["--packet-type", pkt_type] + overwrite_fields
+        res_overwrite = runner.invoke(
+            config.config.commands["switch-hash"].commands["global"].commands[hash_cmd],
+            overwrite_cmd,
+            obj=db,
+        )
+        logger.debug(f"\nOverwrite Command: {hash_cmd} {overwrite_cmd}")
+        logger.debug(f"Output: {res_overwrite.output}")
+        logger.debug(f"Exit code: {res_overwrite.exit_code}")
+        assert res_overwrite.exit_code == SUCCESS
+
+        # Validate that CONFIG_DB now contains exactly overwrite_fields for this pkt_type
+        global_entry = db.cfgdb.get_entry("SWITCH_HASH", "GLOBAL")
+        pkt_key = pkt_type.lower().replace("-", "_")
+        attr_name = f"{attr_prefix}{pkt_key}"
+        value = global_entry.get(attr_name, "")
+        fields = value.split(",") if value else []
+
+        assert fields == overwrite_fields
+
+    def test_debug_ecmp_hash_binding(self):
+        cmd = (
+            config.config
+            .commands["switch-hash"]
+            .commands["global"]
+            .commands["ecmp-hash"]
+        )
+        print("ecmp-hash callback:", cmd.callback, "module:", cmd.callback.__module__)
diff --git a/utilities_common/switch_hash.py b/utilities_common/switch_hash.py
index 9ae50e69..c4707e27 100644
--- a/utilities_common/switch_hash.py
+++ b/utilities_common/switch_hash.py
@@ -17,6 +17,11 @@ SW_CAP_ECMP_HASH_ALGORITHM_CAPABLE_KEY = "ECMP_HASH_ALGORITHM_CAPABLE"
 SW_CAP_LAG_HASH_ALGORITHM_KEY = "LAG_HASH_ALGORITHM"
 SW_CAP_LAG_HASH_ALGORITHM_CAPABLE_KEY = "LAG_HASH_ALGORITHM_CAPABLE"
 
+SW_CAP_ECMP_PKT_TYPE_HASH_CAPABLE_KEY = "ECMP_PKT_TYPE_HASH_CAPABLE"
+SW_CAP_LAG_PKT_TYPE_HASH_CAPABLE_KEY = "LAG_PKT_TYPE_HASH_CAPABLE"
+SW_CAP_ECMP_PKT_TYPE_LIST_KEY = "HASH|ECMP_PKT_TYPE_LIST"
+SW_CAP_LAG_PKT_TYPE_LIST_KEY = "HASH|LAG_PKT_TYPE_LIST"
+
 SW_HASH_KEY = "GLOBAL"
 SW_CAP_KEY = "switch"
 
@@ -39,7 +44,9 @@ HASH_FIELD_LIST = [
     "INNER_SRC_IP",
     "INNER_L4_DST_PORT",
     "INNER_L4_SRC_PORT",
-    "IPV6_FLOW_LABEL"
+    "IPV6_FLOW_LABEL",
+    "RDMA_BTH_OPCODE",
+    "RDMA_BTH_DEST_QP"
 ]
 
 HASH_ALGORITHM = [
@@ -52,6 +59,14 @@ HASH_ALGORITHM = [
     "CRC_XOR"
 ]
 
+PKT_TYPE_LIST = [
+    "IPV4",
+    "IPV6",
+    "IPNIP",
+    "IPV4_RDMA",
+    "IPV6_RDMA"
+]
+
 SYSLOG_IDENTIFIER = "switch_hash"
 
 #
-- 
2.25.1

